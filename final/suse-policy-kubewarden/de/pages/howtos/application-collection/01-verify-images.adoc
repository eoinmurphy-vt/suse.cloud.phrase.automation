include::partial$variables.adoc[]
= Überprüfen der Rancher-Anwendungssammlungs-Images
:revdate: 2026-03-18
:page-revdate: {revdate}
:description: Konfigurieren Sie PolicyServer so, dass sie sich mit einem Docker Config Secret authentifizieren und Images aus der Rancher Application Collection abrufen.
:doc-persona: ["kubewarden-operator", "kubewarden-integrator"]
:doc-topic: ["operator-manual", "rancher", "installation"]
:doc-type: ["howto"]
:keywords: ["{project-name}", "rancher", "application collection", "appco", "signature", "verification", "verify"]
:sidebar_label: Bilder überprüfen
:current-version: {page-origin-branch}

In diesem Abschnitt wird beschrieben, wie Sie Anwendungen und Images aus derhttps://apps.rancher.io/[Application Collection] Rancher mit {project-name} verifizieren.

Sie führen die Image-Überprüfung durch, indem Sie die Richtliniehttps://artifacthub.io/packages/kubewarden/verify-image-signatures/verify-image-signatures[Verify Image Signatures] bereitstellen und die PolicyServer so konfigurieren, dass sie die Signaturschichten der Images aus der Anwendungssammlung beziehen.

[IMPORTANT]
====

Sie benötigen ein Zugriffstoken oder eine Dienstkonto-Authentifizierung für Application Collection.

====


== Authentifizierung bei der Rancher-Anwendungssammlung

Folgen Sie derhttps://docs.apps.rancher.io/get-started/authentication/[Dokumentation zur Anwendungssammlung], erstellen Sie ein Zugriffstoken und konfigurieren Sie den Cluster so, dass er mit einem Docker Config Secret von der Anwendungssammlungsregistrierung zieht:

[subs="+attributes",console]
----
$ kubectl create secret docker-registry application-collection \
  --docker-server=dp.apps.rancher.io \
  --docker-username=<mymail> \
  --docker-password=<mytoken>
----

== Erstellen Sie ein Geheimnis für den PolicyServer, damit dieser aus der Rancher-Anwendungssammlung ziehen kann

Der verwendete PolicyServer muss konfiguriert werden, damit er die Bildebenen, die die Signaturen enthalten, abrufen kann.

Folgen Sie der xref:/howtos/policy-servers/02-private-registry.adoc[Anleitung für private Registrierungen] für Policy Server, um ein Docker Config Secret *im PolicyServer-Namensraum* zu erstellen. Sie tun dies, indem Sie ein Secret mit demselben Inhalt wie das vorherige `application-collection` instanziieren.

Für PolicyServer `default`, der mit dem `kubewarden-defaults` Helm-Diagramm unter dem `kubewarden` -Namensraum installiert wird, wäre dies der Fall:

[subs="+attributes",console]
----
$ kubectl create secret docker-registry application-collection-kw -n kubewarden \
  --docker-server=dp.apps.rancher.io \
  --docker-username=<mymail> \
  --docker-password=<mytoken>
----

== Konfigurieren Sie die PolicyServer so, dass sie das neue Secret verwenden

Wenn Sie den PolicyServer `default` aus dem `kubewarden-defaults` Helm-Diagramm verwenden, konfigurieren Sie ihn mit dem imagePullSecret `application-collection-kw`. Dies geschieht im selben Namensraum und wird durch die Einstellung der folgenden Werte für das Diagramm erreicht:

[subs="+attributes",console]
----
$ helm upgrade -i --wait --namespace kubewarden \
  --create-namespace kubewarden-defaults kubewarden/kubewarden-defaults \
  --reuse-values \
  --set policyServer.imagePullSecret=application-collection-kw
----

Wenn Sie andere PolicyServer verwenden, setzen Sie derenxref:reference/CRDs.adoc#_policyserverspec[`spec.imagePullSecret`]. Der Secret muss sich im gleichen Namespace wie der PolicyServer befinden.

== Die Politik anwenden

Sie können eine ClusterAdmissionPolicy anwenden, indem Sie die Richtliniehttps://artifacthub.io/packages/kubewarden/verify-image-signatures/verify-image-signatures[Verify Image Signatures] verwenden. Diese Richtlinie prüft alle Container-Images von`dp.apps.rancher.io/containers/*`. Die Richtlinie unterstützt OCI-Register und Artefakte.

Sie können die Richtlinieneinstellungen mit demhttps://docs.apps.rancher.io/howto-guides/verify-signatures-with-kubewarden/[öffentlicher Schlüssel] aus der Anwendungssammlung (zum Zeitpunkt des Schreibens) im Array `pubKeys`konfigurieren.

[subs="+attributes",console]
----
$ kubectl apply -f - <<EOF
apiVersion: policies.kubewarden.io/v1
kind: ClusterAdmissionPolicy
metadata:
  annotations:
    artifacthub/pkg: verify-image-signatures/verify-image-signatures/0.2.9
  name: check-appcollection-signatures
spec:
  backgroundAudit: true
  mode: protect
  module: ghcr.io/kubewarden/policies/verify-image-signatures:v0.2.9
  mutating: true
  policyServer: default
  # On first policy call, the policy-server downloads the image layers to
  # verify the signatures. Later on it's cached. It may be useful to increase
  # this timeout:
  timeoutSeconds: 30 # default 10 seconds.
  rules:
    - apiGroups: [""]
      apiVersions: ["v1"]
      resources: ["pods"]
      operations: ["CREATE", "UPDATE"]
    - apiGroups: [""]
      apiVersions: ["v1"]
      resources: ["replicationcontrollers"]
      operations: ["CREATE", "UPDATE"]
    - apiGroups: ["apps"]
      apiVersions: ["v1"]
      resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
      operations: ["CREATE", "UPDATE"]
    - apiGroups: ["batch"]
      apiVersions: ["v1"]
      resources: ["jobs", "cronjobs"]
      operations: ["CREATE", "UPDATE"]
  settings:
    modifyImagesWithDigest: true
    rule: PublicKey
    signatures:
      - image: dp.apps.rancher.io/containers/*
        pubKeys:
          # Note: this array constitutes an AND in validation, not an OR.
          - |-
            -----BEGIN PUBLIC KEY-----
            
            IwIDAQAB
            -----END PUBLIC KEY-----
EOF
----

[subs="+attributes",console]
----
$ kubectl apply -f mypolicy.yml
$ kubectl get admissionpolicies -n default # wait for status active
----

Um dies zu testen, stellen Sie einen Pod mit einem signierten Image aus der Application Collection bereit:

[subs="+attributes",console]
----
$ kubectl run nginx --image [dp.apps.rancher.io/containers/nginx:1.24.0](http://dp.apps.rancher.io/containers/nginx:1.24.0) --overrides='{"spec": {"imagePullSecrets":[{"name": "application-collection"}]}}'
 pod/nginx created
----

Sie können die Protokolle Ihres Policy-Servers Pod einsehen, um zu sehen, dass die Überprüfung stattgefunden hat.
