include::partial$variables.adoc[]
= Recursos criptográficos
:revdate: 2026-03-18
:page-revdate: {revdate}
:description: Saiba mais sobre os recursos e as funções criptográficas do host Wasm para avaliar os certificados de PKI em uso.
:doc-persona: ["kubewarden-policy-developer"]
:doc-topic: ["writing-policies", "specification", "host-capabilities", "cryptographic-capabilities"]
:doc-type: ["reference"]
:keywords: ["kubewarden", "kubernetes", "policy specification", "cryptographic capabilities"]
:sidebar_label: Recursos criptográficos
:current-version: {page-origin-branch}

Devido às restrições do Wasm no momento da redação, algumas bibliotecas criptográficas não podem ser compiladas para o Wasm. Enquanto isso, as políticas do {project-name} que precisam dessas bibliotecas podem executar retornos de chamada para avaliar as funções criptográficas no lado do host. Eles recebem o resultado e continuam com suas tarefas.

== Comportamento

O host Wasm conta com o site `rustls-wepki` para a implementação de certificados x.509 e PKI. Isso significa que:

- Uso principal: Optamos por aceitar qualquer uso de chave, o que ajuda a cobrir vários casos de uso (como sempre, entre em contato se quiser discutir abordagens diferentes).
- Cadeias de certificados: Um certificado será considerado não confiável se sua CA intermediária estiver expirada.
- Datas de expiração do certificado: Se uma cadeia de certificados for fornecida, sempre validaremos todo o período de validade do certificado. O Wepki não verifica a expiração da CA raiz.
- Sempre validamos o certificado e, se nenhuma cadeia for fornecida, a CA da Mozilla será usada.

== Contrato de protocolo WaPC

Se você estiver implementando seu próprio SDK de linguagem, essas são as funções que executam verificações criptográficas expostas pelo host:

=== Função waPC - `v1/is_certificate_trusted` input

[,hcl]
----
# Certificate:
{
  # **mandatory**: Which encoding is used by the certificate
  # Either the string "Pem" or "Der".
  "encoding": string,
  # Actual certificate
  # The certificate is UTF-8 encoded.
  # It's an array of bytes of the unicode code pointers of a PEM/DER encoded
  # certificate string.
  "data": [byte(int), ..., byte(int)]
}

{
  # **mandatory**: PEM-encoded certificate to verify
  "cert": Certificate,
  # optional:
  "cert_chain": [
      # list of certs, ordered by trust
      # usage (intermediates first, root last)
      # If not provided, Mozilla's CA is used.
      Certificate,
      ...
      Certificate,
    ],
  # RFC 3339 time format string, to check expiration
  # against.
  # If missing, time is now.
  "not_after": string
}
----

=== Função waPC - `v1/is_certificate_trusted` output

[,hcl]
----
{
   # true if certificate verified:
   "trusted": boolean,
   # empty if trusted == true:
   "reason": string
}
----
