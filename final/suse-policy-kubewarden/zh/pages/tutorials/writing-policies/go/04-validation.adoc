include::partial$variables.adoc[]
= 编写验证逻辑
:revdate: 2026-03-18
:page-revdate: {revdate}
:description: 学习如何从传入的有效载荷中提取相关信息并创建设置对象，从而在 Go 中编写和测试验证逻辑。
:doc-persona: ["kubewarden-policy-developer"]
:doc-topic: ["kubewarden", "writing-policies", "golang", "validation-logic"]
:doc-type: ["tutorial"]
:keywords: ["kubewarden"、"kubernetes"、"编写策略"、"golang"、"go"]
:sidebar_label: 验证逻辑
:sidebar_position: 32
:current-version: {page-origin-branch}

验证逻辑将在`validate.go` 文件中进行。

您的验证逻辑需要

* 从传入的`payload` 对象中提取相关信息。
* 根据输入和策略设置返回响应。

传入的有效载荷是一个 JSON 对象，xref:reference/spec/03-validating-policies.adoc[如本文档]所述，您可以通过两种方式从中获取数据：

. 将 JSON 数据解码为 Go 类型。
. 执行 JSON 查询（类似于https://stedolan.github.io/jq/[`jq`]).

本节文档主要介绍第一种方法，即使用 Go 类型。
第二种方法将在后面的xref:tutorials/writing-policies/go/09-validation-with-queries.adoc[查询验证]部分进行介绍。

[NOTE]
====

依靠 Kubernetes 对象，而不是进行类似 jq 的搜索，会导致生成更大的 WebAssembly 模块。
使用 Kubernetes 对象的策略约为 1.5&nbsp;MB，而使用`gjson` 的策略约为 300&nbsp;KB。

除了大小之外，使用 Kubernetes 对象的策略在首次执行时需要更多时间。
由于{project-name} 使用了 Wasmtime 的缓存功能，因此后续调用速度很快。
第一次执行约需 20 秒（`kwctl` ），以后的执行只需 1 到 2 秒。
因此，{short-project-name} Policy Server 的启动时间较慢，但使用 Kubernetes 对象通常不会影响策略评估时间。
====


== `validate` 功能

在`validate.go` 中，脚手架模板提供的策略已经具有`validate` 功能。
在本教程中，您需要对它做一些改动。

功能完成后应该是这样的：

[,go]
----
func validate(payload []byte) ([]byte, error) {
    // NOTE 1
    // Create a ValidationRequest instance from the incoming payload
    validationRequest := kubewarden_protocol.ValidationRequest{}
    err := json.Unmarshal(payload, &validationRequest)
    if err != nil {
        return kubewarden.RejectRequest(
            kubewarden.Message(err.Error()),
            kubewarden.Code(400))
    }

    // NOTE 2
    // Create a Settings instance from the ValidationRequest object
    settings, err := NewSettingsFromValidationReq(&validationRequest)
    if err != nil {
        return kubewarden.RejectRequest(
            kubewarden.Message(err.Error()),
            kubewarden.Code(400))
    }

    // NOTE 3
    // Access the **raw** JSON that describes the object
    podJSON := validationRequest.Request.Object

    // NOTE 4
    // Try to create a Pod instance using the RAW JSON we got from the
    // ValidationRequest.
    pod := &corev1.Pod{}
    if err := json.Unmarshal([]byte(podJSON), pod); err != nil {
        return kubewarden.RejectRequest(
            kubewarden.Message(
                fmt.Sprintf("Cannot decode Pod object: %s", err.Error())),
            kubewarden.Code(400))
    }

    logger.DebugWithFields("validating pod object", func(e onelog.Entry) {
        e.String("name", pod.Metadata.Name)
        e.String("namespace", pod.Metadata.Namespace)
    })

    // NOTE 5
    for label, value := range pod.Metadata.Labels {
        if err := validateLabel(label, value, &settings); err != nil {
            return kubewarden.RejectRequest(
                kubewarden.Message(err.Error()),
                kubewarden.NoCode)
        }
    }

    return kubewarden.AcceptRequest()
}
----

该代码有`NOTE` 部分：

. 通过解除对 JSON 有效负载的标记，创建`kubewarden_protocol.ValidationRequest` 。
. 使用之前在`settings.go` 文件中定义的函数，创建`Settings` 对象。
. 访问作为`ValidationRequest` 一部分的 Pod 的原始 JSON 表示。
. 解除 Pod 对象的封印。
. 迭代 Pod 的标签。
您可以使用名为`validateLabel` 的新函数来识别违反策略的标签。

您还需要在`validate.go` 文件中定义`validateLabel` 函数：

[,go]
----
func validateLabel(label, value string, settings *Settings) error {
    if settings.DeniedLabels.Contains(label) {
        return fmt.Errorf("Label %s is on the deny list", label)
    }

    regExp, found := settings.ConstrainedLabels[label]
    if found {
        // This is a constrained label
        if !regExp.Match([]byte(value)) {
            return fmt.Errorf("The value of %s doesn't pass user-defined constraint", label)
        }
    }

    return nil
}
----

== 测试验证码

现在，您可以编写单元测试来检查验证代码的运行情况。
在`validate_test.go` 文件中找到测试。

您应该替换脚手架文件的内容，使之与之匹配：

.`validate_test.go`
[%collapsible]
======

[,go]
----
package main

import (
    "regexp"
    "testing"

    "encoding/json"

    mapset "github.com/deckarep/golang-set/v2"
    corev1 "github.com/kubewarden/k8s-objects/api/core/v1"
    metav1 "github.com/kubewarden/k8s-objects/apimachinery/pkg/apis/meta/v1"
    kubewarden_protocol "github.com/kubewarden/policy-sdk-go/protocol"
    kubewarden_testing "github.com/kubewarden/policy-sdk-go/testing"
)

func TestValidateLabel(t *testing.T) {
    // NOTE 1
    cases := []struct {
        podLabels         map[string]string
        deniedLabels      mapset.Set[string]
        constrainedLabels map[string]*RegularExpression
        expectedIsValid   bool
    }{
        {
            // ➀
            // Pod has no labels -> should be accepted
            podLabels:         map[string]string{},
            deniedLabels:      mapset.NewThreadUnsafeSet[string]("owner"),
            constrainedLabels: map[string]*RegularExpression{},
            expectedIsValid:   true,
        },
        {
            // ➁
            // Pod has labels, none is denied -> should be accepted
            podLabels: map[string]string{
                "hello": "world",
            },
            deniedLabels:      mapset.NewThreadUnsafeSet[string]("owner"),
            constrainedLabels: map[string]*RegularExpression{},
            expectedIsValid:   true,
        },
        {
            // ➂
            // Pod has labels, one is denied -> should be rejected
            podLabels: map[string]string{
                "hello": "world",
            },
            deniedLabels:      mapset.NewThreadUnsafeSet[string]("hello"),
            constrainedLabels: map[string]*RegularExpression{},
            expectedIsValid:   false,
        },
        {
            // ➃
            // Pod has labels, one has constraint that is respected -> should be accepted
            podLabels: map[string]string{
                "cc-center": "team-123",
            },
            deniedLabels: mapset.NewThreadUnsafeSet[string]("hello"),
            constrainedLabels: map[string]*RegularExpression{
                "cc-center": {
                    Regexp: regexp.MustCompile(`team-\d+`),
                },
            },
            expectedIsValid: true,
        },
        {
            // ➄
            // Pod has labels, one has constraint that are not respected -> should be rejected
            podLabels: map[string]string{
                "cc-center": "team-kubewarden",
            },
            deniedLabels: mapset.NewThreadUnsafeSet[string]("hello"),
            constrainedLabels: map[string]*RegularExpression{
                "cc-center": {
                    Regexp: regexp.MustCompile(`team-\d+`),
                },
            },
            expectedIsValid: false,
        },
        {
            // ➅
            // Settings have a constraint, pod doesn't have this label -> should be rejected
            podLabels: map[string]string{
                "owner": "team-kubewarden",
            },
            deniedLabels: mapset.NewThreadUnsafeSet[string]("hello"),
            constrainedLabels: map[string]*RegularExpression{
                "cc-center": {
                    Regexp: regexp.MustCompile(`team-\d+`),
                },
            },
            expectedIsValid: false,
        },
    }

    // NOTE 2
    for _, testCase := range cases {
        settings := Settings{
            DeniedLabels:      testCase.deniedLabels,
            ConstrainedLabels: testCase.constrainedLabels,
        }

        pod := corev1.Pod{
            Metadata: &metav1.ObjectMeta{
                Name:      "test-pod",
                Namespace: "default",
                Labels:    testCase.podLabels,
            },
        }

        payload, err := kubewarden_testing.BuildValidationRequest(&pod, &settings)
        if err != nil {
            t.Errorf("Unexpected error: %+v", err)
        }

        responsePayload, err := validate(payload)
        if err != nil {
            t.Errorf("Unexpected error: %+v", err)
        }

        var response kubewarden_protocol.ValidationResponse
        if err := json.Unmarshal(responsePayload, &response); err != nil {
            t.Errorf("Unexpected error: %+v", err)
        }

        if testCase.expectedIsValid && !response.Accepted {
            t.Errorf("Unexpected rejection: msg %s - code %d with pod labels: %v, denied labels: %v, constrained labels: %v",
                *response.Message, *response.Code, testCase.podLabels, testCase.deniedLabels, testCase.constrainedLabels)
        }

        if !testCase.expectedIsValid && response.Accepted {
            t.Errorf("Unexpected acceptance with pod labels: %v, denied labels: %v, constrained labels: %v",
                testCase.podLabels, testCase.deniedLabels, testCase.constrainedLabels)
        }
    }
}
----

======

测试采用 "测试用例驱动 "方法。
首先要定义一个`struct` ，用于保存测试用例所需的数据，请参见`NOTE 1` ：

[,go]
----
struct {
        podLabels         map[string]string
        deniedLabels      mapset.Set[string]
        constrainedLabels map[string]*RegularExpression
        expectedIsValid   bool
}
----

然后声明几个测试用例。
在上面可折叠的大代码块中，它们的起始行标记为 ➀ 至 ➅。

例如，您应该认为没有标签的 Pod 是有效的。
您可以使用这些输入值进行测试：

[,go]
----
{
  podLabels:         map[string]string{},
  deniedLabels:      mapset.NewThreadUnsafeSet[string]("owner"),
  constrainedLabels: map[string]*RegularExpression{},
  expectedIsValid:   true,
}
----

测试以这种方式定义新的情景，直到`NOTE 2` 。
在这里，您可以使用以下代码遍历不同的测试用例：

. 使用`testCase` 提供的数据创建`BasicSettings` 对象。
. 创建`Pod` 对象，将`testCase` 中定义的标签分配给它。
. 创建`payload` 对象。使用{short-project-name} SDK 的一个辅助函数来实现：`kubewarden_testing.BuildValidationRequest` 。
该函数的输入是请求涉及的对象`Pod` 和描述设置的对象`BasicSettings` 实例。
. 最后，代码会调用`validate` 函数，并对结果进行检查。

现在，您可以使用`settings_test.go` 运行所有单元测试，包括在 中定义的测试：

[,console]
----
make test
----

输出结果如下

.输出 `make test`
[%collapsible]
======

[,shell]
----
make test
go test -v
=== RUN   TestParsingSettingsWithNoValueProvided
--- PASS: TestParsingSettingsWithNoValueProvided (0.00s)
=== RUN   TestIsNameDenied
--- PASS: TestIsNameDenied (0.00s)
=== RUN   TestParseValidSettings
--- PASS: TestParseValidSettings (0.00s)
=== RUN   TestParseSettingsWithInvalidRegexp
--- PASS: TestParseSettingsWithInvalidRegexp (0.00s)
=== RUN   TestDetectValidSettings
--- PASS: TestDetectValidSettings (0.00s)
=== RUN   TestDetectNotValidSettingsDueToBrokenRegexp
--- PASS: TestDetectNotValidSettingsDueToBrokenRegexp (0.00s)
=== RUN   TestDetectNotValidSettingsDueToConflictingLabels
--- PASS: TestDetectNotValidSettingsDueToConflictingLabels (0.00s)
=== RUN   TestValidateLabel
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
    validate_test.go:126: Unexpected acceptance with pod labels: map[owner:team-kubewarden], denied labels: Set{hello}, constrained labels: map[cc-center:team-\d+]
--- FAIL: TestValidateLabel (0.00s)
FAIL
exit status 1
FAIL    github.com/kubewarden/go-policy-template        0.003s
make: *** [Makefile:29: test] Error 1
----

======

可以看到，所有`Settings` 测试都通过了，但`TestValidateLabel` 有一个测试用例没有通过：

[,console]
----
validate_test.go:126: Unexpected acceptance with pod labels: map[owner:team-kubewarden], denied labels: Set{hello}, constrained labels: map[cc-center:team-\d+]
----

在这种情况下，您的策略设置要求 Pod 必须有一个标签，其关键字`cc-center` 必须满足`team-\d+` 正则表达式。
测试的花苞没有这个标签，所以你应该拒绝它。
但这并没有发生，因此您可以在下一节中解决这个问题。

[NOTE]
====
你可能想知道，为什么单元测试的输出会出现以下行文
`NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}`

政策中的`logger` 语句会产生该输出。
只有当代码在 WebAssembly 上下文之外运行时，才会出现这种情况。
在{short-project-name} 中评估策略时不会出现这种情况，在这种情况下，`logger` 语句会发出 OpenTelemetry 事件。
====


=== 修复损坏的单元测试

要修复您发现的测试错误，您必须更改验证函数，`validate` `validate.go` 。

目前，验证逻辑的核心是以下几行：

[,go]
----
for label, value := range pod.Metadata.Labels {
    if err := validateLabel(label, value, &settings); err != nil {
        return kubewarden.RejectRequest(
            kubewarden.Message(err.Error()),
            kubewarden.NoCode)
    }
}
----

在这里，你要遍历每个标签，检查它是否未被拒绝，是否违反了用户指定的某个限制条件。
但是，您并没有确保 Pod 拥有`Settings.ConstrainedLabels` 中指定的所有标签。

在`for` 循环之后添加新代码：

[,go]
----
for requiredLabel := range settings.ConstrainedLabels {
    _, found := pod.Metadata.Labels[requiredLabel]
    if !found {
        return kubewarden.RejectRequest(
            kubewarden.Message(fmt.Sprintf(
                "Constrained label %s not found inside of Pod",
                requiredLabel),
            ),
            kubewarden.NoCode)
    }
}
----

再次运行单元测试：

[,console]
----
make test
----

此输出：

.最终输出 `make test`
[%collapsible]
======

[,console]
----
make test
go test -v
=== RUN   TestParsingSettingsWithNoValueProvided
--- PASS: TestParsingSettingsWithNoValueProvided (0.00s)
=== RUN   TestIsNameDenied
--- PASS: TestIsNameDenied (0.00s)
=== RUN   TestParseValidSettings
--- PASS: TestParseValidSettings (0.00s)
=== RUN   TestParseSettingsWithInvalidRegexp
--- PASS: TestParseSettingsWithInvalidRegexp (0.00s)
=== RUN   TestDetectValidSettings
--- PASS: TestDetectValidSettings (0.00s)
=== RUN   TestDetectNotValidSettingsDueToBrokenRegexp
--- PASS: TestDetectNotValidSettingsDueToBrokenRegexp (0.00s)
=== RUN   TestDetectNotValidSettingsDueToConflictingLabels
--- PASS: TestDetectNotValidSettingsDueToConflictingLabels (0.00s)
=== RUN   TestValidateLabel
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
NATIVE: |{"level":"debug","message":"validating pod object","name":"test-pod","namespace":"default"}
|
--- PASS: TestValidateLabel (0.00s)
PASS
ok      github.com/kubewarden/go-policy-template        0.003s
----

======

正如您所看到的，这次所有测试都通过了。
现在可以进入下一步，编写端到端测试。
