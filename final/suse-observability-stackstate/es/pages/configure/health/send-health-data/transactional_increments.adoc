= Elementos transaccionales
:revdate: 2025-10-27
:page-revdate: {revdate}
:description: Observabilidad de SUSE

== Visión general

En esta página se describen los mensajes JSON exactos que se pueden enviar para el modelo de coherencia de la sincronización de salud de los incrementos transaccionales.

== Propiedad JSON: "health"

La salud puede enviarse a la API de SUSE Observability Receiver mediante la propiedad `"health"` del xref:/configure/health/send-health-data/send-health-data.adoc#_common_json_object[objeto JSON común].

[tabs]
====
Ejemplo de salud `transactional_increments` JSON::
+
--

[,javascript]
----
   "apiKey":"your api key",
   "collection_timestamp":1585818978,
   "internalHostname":"lnx-343242.srv.stackstate.com",
   "events":{},
   "metrics":[],
   "service_checks":[],
   "health":[
      {
        "consistency_model": "TRANSACTIONAL_INCREMENTS",
        "increment": {
              "checkpoint": {
                  "offset": 5,
                  "batch_index": 102
              },
              "previous_checkpoint": {
                  "offset": 5,
                  "batch_index": 100
              }
        },
        "stream": {
          "urn": "urn:health:sourceId:streamId"
          //"sub_stream_id": "subStreamId" Optional
        },
        "check_states": [
          {
            "checkStateId": "checkStateId1",
            "message": "Server Running out of disk space",
            "health": "Deviating",
            "topologyElementIdentifier": "server-1",
            "name": "Disk Usage"
          },
          {
            "checkStateId": "checkStateId2",
            "message": "Provisioning failed. [Learn more](https://www.any-link.com)",
            "health": "critical",
            "topologyElementIdentifier": "server-2",
            "name": "Health monitor"
          },
          {
            "checkStateId": "checkStateId3",
            "delete": true
          }
        ]
      }
   ],
   "topologies":[]
----

--
====

Cada carga útil de datos de incrementos transaccionales de salud tiene los siguientes detalles:

* *Incremento* - En cada mensaje debe haber un objeto de incremento. Esto permite a SUSE Observability realizar un seguimiento de la cadena completa de mensajes y ser capaz de detectar cuándo se está produciendo una retransmisión de datos, o una brecha inesperada en los datos. Lleva los siguientes campos como metadatos de incremento:
 ** *checkpoint* - Objeto que proporciona el checkpoint al que pertenece el `check_states` presente en el mensaje, tiene dos campos:
  *** *offset* - El offset asignado a los mensajes por el canal de streaming. Por ejemplo, Kafka offset.
  *** *batch_index* - Optional. Cuando se utiliza un único mensaje para acumular varios `check_states`, el índice del lote representa el último índice presente en el mensaje, lo que permite enviar grandes lotes en llamadas api separadas.
 ** *previous_checkpoint* - Opcional. Representa el punto de control comunicado previamente, puede estar vacío en la primera transmisión del subflujo. Permite a SUSE Observability realizar un seguimiento en caso de que pudiera faltar algún dato en el flujo ascendente.
* *stream* - Objeto que proporciona identificación sobre qué instantáneas y `check_states` pertenecen juntas. Dispone de los siguientes campos:
 ** *urn* - ID de fuente y flujo de datos codificado como un xref:/configure/topology/identifiers.adoc[URN de] SUSE Observability que coincide con la siguiente convención: `urn:health:<sourceId>:<streamId>` donde `<sourceId>` es el nombre de la fuente de datos externa y `<streamId>` es un identificador único para el flujo de datos de salud.
 ** *sub_stream_id* - Optional. Identificador de un subconjunto de los datos sanitarios del flujo. Cuando los datos del flujo son distribuidos y comunicados por varios agentes, esto permite ciclos de vida instantáneos por `sub_stream_id`
* *check_states* - Lista de estados de comprobación. Cada estado de control puede tener los siguientes campos:
 ** *checkStateId* - Identificador del estado de comprobación en el sistema externo
 ** *mensaje* - Opcional. Mensaje que se mostrará en la interfaz de usuario de SUSE Observability. Los datos se interpretarán como markdown permitiendo tener enlaces a la comprobación del sistema externo que generó el estado de comprobación externa.
 ** *health* - Uno de los siguientes valores de estado de SUSE Observability Health: `Clear`, `Deviating`, `Critical`.
 ** *topologyElementIdentifier* - Se utiliza para vincular el estado de comprobación a un elemento de topología de SUSE Observability.
 ** *name* - Nombre del estado de comprobación externa.
 ** *delete* - Bandera que se interpreta como una solicitud de borrado de la página `checkStateId`. Incluso si el resto de los campos para la creación están presentes, por ejemplo, `name, health, ...` la eliminación tendrá prioridad.

== Enviar salud a SUSE Observability

La salud puede enviarse en un mensaje JSON a través de HTTP POST. En el siguiente ejemplo, se envía a SUSE Observability una instantánea que contiene dos estados de comprobación desde un único sistema de supervisión externo.

[,bash]
----
curl -X POST \
 '<STACKSTATE_RECEIVER_API_ADDRESS>' \
 -H 'Content-Type: application/json' \
 -d '{
  "collection_timestamp": 1548857167,
  "internalHostname": "local.test",
  "health": [
    {
      "consistency_model": "TRANSACTIONAL_INCREMENTS",
      "increment": {
            "checkpoint": {
                "offset": 5,
                "batch_index": 102
            },
            "previous_checkpoint": {
                "offset": 5,
                "batch_index": 100
            }
      },
      "stream": {
        "urn": "urn:health:sourceId:streamId"
      },
      "check_states": [
        {
          "checkStateId": "checkStateId1",
          "message": "Server Running out of disk space",
          "health": "Deviating",
          "topologyElementIdentifier": "server-1",
          "name": "Disk Usage"
        },
        {
          "checkStateId": "checkStateId2",
          "message": "Provisioning failed. [Learn more](https://www.any-link.com)",
          "health": "critical",
          "topologyElementIdentifier": "server-2",
          "name": "Health monitor"
        },
        {
          "checkStateId": "checkStateId3",
          "delete": true
        }
      ]
    }
  ]
}'
----
