= Introducción a AWS Lambda
:revdate: 2025-07-10
:page-revdate: {revdate}
:description: Observabilidad de SUSE

Configurar la monitorización de una o varias funciones de AWS Lambda:

* La(s) función(es) AWS Lambda monitorizada(s) (instrumentada(s) mediante Open Telemetry)
* El colector de telemetría abierto
* SUSE Observability o SUSE Cloud Observability

image::otel/open-telemetry-collector-lambda.png[Instrumentación de AWS Lambda con Open Telemetry con el recopilador Open Telemetry ejecutándose en Kubernetes]

== El colector de telemetría abierto

[NOTE]
====
Para una configuración de producción, se recomienda encarecidamente instalar el recopilador, ya que permite que su servicio descargue datos rápidamente y el recopilador puede encargarse de tareas adicionales como reintentos, procesamiento por lotes, cifrado o incluso filtrado de datos confidenciales.
====


Instala el recolector OTel (Open Telemetry), en este ejemplo usamos un clúster Kubernetes para ejecutarlo cerca de las funciones Lambda. Se puede realizar una configuración similar utilizando un recopilador instalado en una máquina virtual. La configuración utilizada aquí solo actúa como proxy seguro para descargar datos rápidamente de las funciones Lambda y se ejecuta dentro de una infraestructura de red de confianza.

=== Crear un token de servicio

Hay dos formas de crear un token de servicio:

* **SUSE Observability UI** - abra el menú principal haciendo clic en la parte superior izquierda de la pantalla y vaya a `StackPacks` > `Open Telemetry`.  Si no lo ha hecho antes, pulse el botón `INSTALL`. Haga clic en el botón `CREATE NEW SERVICE TOKEN` y copie el valor en el portapapeles.
* **SUSE Observability CLI**: consulte xref:/use/security/k8s-service-tokens.adoc#_manage_service_tokens[Gestión de tokens de servicio]

El valor del testigo de servicio debe utilizarse cuando en las instrucciones siguientes se mencione `<SERVICE_TOKEN>`.

=== Crear el espacio de nombres y un secreto para el token de servicio

Instalar en el espacio de nombres `open-telemetry` y utilizar el token de servicio:

[,bash]
----
kubectl create namespace open-telemetry
kubectl create secret generic open-telemetry-collector \
    --namespace open-telemetry \
    --from-literal=API_KEY='<SERVICE_TOKEN>'
----

=== Configurar e instalar el colector

Instalamos el colector con una carta Helm proporcionada por el proyecto Open Telemetry. Asegúrese de que tiene configurado el repositorio de cartas de timón de Open Telemetry:

[,bash]
----
helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
----

Cree un archivo de valores `otel-collector.yaml` para el gráfico Helm. Este es un buen punto de partida para su uso con SUSE Observability, sustituya `<otlp-suse-observability-endpoint:port>` por su punto final xref:/setup/otel/otlp-apis.adoc[OTLP] (consulte la xref:/setup/otel/otlp-apis.adoc[API OTLP] para su punto final) e inserte el nombre de su clúster Kubernetes en lugar de `<your-cluster-name>`. Cuando utilice la configuración de entrada, asegúrese también de insertar su propio nombre de dominio y el correspondiente secreto del certificado TLS en las ubicaciones marcadas.

.otel-collector.yaml
[,yaml]
----
mode: deployment
presets:
  kubernetesAttributes:
    enabled: true
    # You can also configure the preset to add all the associated pod's labels and annotations to you telemetry.
    # The label/annotation name will become the resource attribute's key.
    extractAllPodLabels: true
extraEnvsFrom:
  - secretRef:
      name: open-telemetry-collector
image:
  repository: "ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-k8s"

config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318
  extensions:
    # Use the API key from the env for authentication
    bearertokenauth:
      scheme: SUSEObservability
      token: "${env:API_KEY}"
  exporters:
    otlp:
      auth:
        authenticator: bearertokenauth
      # Put in your own otlp endpoint, for example otlp-suse-observability.my.company.com:443
      endpoint: <otlp-suse-observability-endpoint:port>

  service:
    extensions: [health_check, bearertokenauth]
    pipelines:
      traces:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]
      metrics:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]
      logs:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: ingress-traefik-external
    traefik.ingress.kubernetes.io/ingress.class: ingress-traefik-external
    traefik.ingress.kubernetes.io/backend-protocol: GRPC
    # "12.34.56.78/32" IP address of NatGateway in the VPC where the otel data is originating from
    #  traefik.ingress.kubernetes.io/whitelist-source-range: "12.34.56.78/32"
  hosts:
    - host: "otlp-collector-proxy.<your-domain>"
      paths:
        - path: /
          pathType: ImplementationSpecific
          port: 4317
  tls:
    - secretName: <secret-for-tls-certificate>
      hosts:
        - "otlp-collector-proxy.<your-domain>"

# Instead of ingress:

# Alternative 1, load balancer service
#service:
#  type: LoadBalancer
#  loadBalancerSourceRanges: 12.34.56.78/32 # The IP address of NatGateway in the VPC for the lambda functions

# Alternative 2, node port service
#service:
#  type: NodePort
#ports:
#  otlp:
#    nodePort: 30317
----


Ahora instale el colector, utilizando el archivo de configuración:

[,bash]
----
helm upgrade --install opentelemetry-collector open-telemetry/opentelemetry-collector \
  --values otel-collector.yaml \
  --namespace open-telemetry
----

Asegúrese de que las funciones Lambda puedan acceder al recopilador proxy, ya sea mediante el acceso público a la entrada o mediante la IP del recopilador en la misma VPC que las funciones Lambda.  Se recomienda utilizar una lista blanca de fuentes para filtrar los datos procedentes de fuentes no fiables y/o desconocidas (véase el comentario en el yaml). Además de la configuración de entrada, también es posible exponer el recopilador a las funciones Lambda a través de:

* un servicio LoadBalancer que restringe el acceso limitando los rangos de origen, véase "Alternativa 1".
* un servicio NodePort para el recopilador, consulte "Alternativa 2".

El xref:/setup/otel/collector.adoc[recopilador] ofrece muchos más receptores de configuración, procesadores y exportadores, para más detalles consulte nuestra xref:/setup/otel/collector.adoc[página del recopilador]. Para el uso en producción, a menudo se generan grandes cantidades de tramos y querrá empezar a configurar el xref:/setup/otel/sampling.adoc[muestreo].

== Instrumentar una función lambda

Open Telemetry soporta la instrumentación de funciones Lambda en múltiples lenguajes utilizando capas Lambda. La configuración de esas capas Lambda debe utilizar la dirección del recopilador del paso anterior para enviar los datos. Para instrumentar un Node.js lambda siga nuestras xref:/setup/otel/instrumentation/node.js/auto-instrumentation-of-lambdas.adoc[instrucciones detalladas aquí]. Para instrumentar otros lenguajes aplique la misma configuración que para Node.js pero utilice una de las otras https://opentelemetry.io/docs/platforms/faas/lambda-auto-instrument/[capas Lambda de].

== Ver los resultados

Vaya a SUSE Observability y asegúrese de que el Stackpack Open Telemetry está instalado (a través del menú principal \-> Stackpacks).

Después de un corto tiempo y si tu(s) función(es) Lambda están recibiendo algo de tráfico, deberías ser capaz de encontrar las funciones bajo su nombre de servicio en la Telemetría Abierta \-> servicios e instancias de servicio. Las trazas aparecerán en el xref:/use/traces/k8sTs-explore-traces.adoc[explorador de trazas] y en la xref:/use/views/k8s-traces-perspective.adoc[perspectiva de trazas] para los componentes de servicio e instancia de servicio. Las métricas de alcance y las métricas específicas del idioma (si están disponibles) estarán disponibles en la xref:/use/views/k8s-metrics-perspective.adoc[perspectiva de métricas] de los componentes.

== Próximos pasos

ifdef::ss-ff-stackpacks2_enabled[]
Puede añadir nuevos gráficos a los componentes, por ejemplo, el servicio o la instancia de servicio, para su aplicación, siguiendo xref:/setup/custom-integrations/metric-bindings/index.adoc[nuestra guía]. También es posible crear xref:/use/alerting/k8s-monitors.adoc[nuevos monitores] utilizando las métricas y configurar xref:/use/alerting/notifications/configure.adoc[notificaciones] para recibir avisos cuando la aplicación no esté disponible o tenga problemas de rendimiento.
endif::[]

ifndef::ss-ff-stackpacks2_enabled[]
Puede añadir nuevos gráficos a los componentes, por ejemplo el servicio o la instancia de servicio, para su aplicación, siguiendo xref:/use/metrics/k8s-add-charts.adoc[nuestra guía]. También es posible crear xref:/use/alerting/k8s-monitors.adoc[nuevos monitores] utilizando las métricas y configurar xref:/use/alerting/notifications/configure.adoc[notificaciones] para recibir avisos cuando la aplicación no esté disponible o tenga problemas de rendimiento.
endif::[]

== Seguir leyendo

* xref:/use/security/k8s-service-tokens.adoc[Fichas de servicio]
* xref:/setup/otel/otlp-apis.adoc[API de telemetría abierta]
* xref:/setup/otel/collector.adoc[Personalización de la configuración del colector abierto de telemetría]
* xref:/setup/otel/instrumentation/README.adoc[SDK abiertos de telemetría]
