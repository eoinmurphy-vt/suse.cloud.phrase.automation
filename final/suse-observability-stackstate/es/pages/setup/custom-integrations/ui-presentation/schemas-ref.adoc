ifdef::ss-ff-stackpacks2_enabled[]
= Esquema de presentación de componentes
:revdate: 2026-01-08
:page-revdate: {revdate}
:description: Esquema de presentación de componentes

[IMPORTANT]
====
*Estado:* En desarrollo activo.

*Disponibilidad:* Detrás de la bandera de características de StackPacks 2.0. Consulte la xref:/setup/custom-integrations/overview.adoc[Descripción general de las integraciones personalizadas] para saber cómo activar la función.

Esta documentación describe la intención de diseño actual y la superficie de configuración de alto nivel.

Nota: aún no se ha implementado ningún comportamiento ni capacidad.

Como esta función está en desarrollo activo, el comportamiento, los esquemas y las capacidades pueden cambiar a medida que evoluciona el modelo.
====

== Visión general

Esta página documenta el esquema de configuración utilizado por el modelo de presentación de la interfaz de usuario.
Está pensado como referencia para los autores que definen el comportamiento de presentación de la interfaz de usuario.

Todos los esquemas descritos aquí forman parte de *StackPacks 2.0* y están en desarrollo activo.

== Páginas de interfaz de usuario rellenadas por `ComponentPresentation`

Consulte las siguientes capturas de pantalla para hacerse una idea de a qué páginas y elementos de la interfaz de usuario de las respectivas páginas pertenece la configuración de `ComponentPresentation`.

=== Página general de tipos de componentes

En la referencia del esquema, véase la sección `overview`.

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_overview_page.png[Página general]

=== Componente Página destacada

En la referencia del esquema, véase la sección `highlight`.

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_highlight_page.png[Página destacada]

== ComponentPresentation

En `ComponentPresentation` se define cómo se presentan los componentes coincidentes en la interfaz de usuario para un modo y un contexto determinados.

[,yaml]
----
_type: ComponentPresentation
identifier: urn:...:component-presentation:<presentation-name>
mode: ["observability" | "security" | "all"]

binding:
  _type: ComponentPresentationQueryBinding
  query: string                       # Primitive STQL query used to match components

rank:
  specificity: number                 # Higher values indicate more specific definitions

presentation:
  icon: string                        # Optional icon (base64-encoded)
  name:                               # Optional naming configuration
    plural: string
    singular: string
    title: string

  mainMenu:                           # Optional main menu entry
    group: string                     # Name of an existing MainMenuGroup
    rank: number                      # Optional override within the group

  overview:                           # Optional overview page configuration
    columns:
      - _type: string                 # ViewTypeTableCol*

  highlight:                          # Optional highlight page configuration
    summary:
      ...
    fields:
      - _type: string                 # ComponentTypeField
        fieldId: string
        label:
          title: string
          helpBubbleText: string      # Optional
        valueExtractor:
          _type: string
        display:
          _type: string
    about:
      _type: ComponentTypeAbout
      fields:
        - string

  filter:                             # Optional overview filters
    - _type: FilterField
      name: string
      tagField: string
----

== Identificador

Los identificadores deben seguir el formato del identificador {stackstate-product-name} (es decir, `urn:...`). Consulte la documentación xref:/configure/topology/identifiers.adoc[sobre identificadores] para obtener más información.

== Encuadernación

La vinculación determina a qué componentes se aplica un `ComponentPresentation`.

Los enlaces se evalúan en función de los datos del componente y deben ser sencillos y eficientes, ya que se utilizan durante la evaluación de la interfaz de usuario.

Tipo actualmente admitido `binding`:

* `ComponentPresentationQueryBinding`una consulta STQL (consulte xref:/develop/reference/k8sTs-stql_reference.adoc[STQL] para obtener más información)

== Rango

La sección `rank` controla cómo se combinan las definiciones coincidentes de `ComponentPresentation`.

Los valores de especificidad más altos indican definiciones más especializadas y tienen prioridad sobre los valores más bajos a la hora de componer el comportamiento de presentación.

== Secciones de presentación

Todos los campos de `presentation` son opcionales. Un sitio `ComponentPresentation` puede definir sólo un subconjunto de aspectos de la presentación.

Las secciones de presentación se componen cuando se aplican varias definiciones al mismo componente.

=== Icono y nombre

Define la iconografía y los nombres utilizados en la interfaz de usuario.

=== Menú principal

Controla si los componentes coincidentes aparecen en el menú principal.

El grupo de menús al que se hace referencia debe definirse por separado mediante `MainMenuGroup`.

=== Visión general

Define las columnas que aparecen en los cuadros sinópticos.

Las columnas pueden anular o ampliar las columnas definidas por otras presentaciones coincidentes.

=== Destaque

Define los campos y secciones que aparecen en las páginas destacadas.

==== Resumen

==== Campos

==== Acerca de

Define los campos que se muestran en el panel "Acerca de" de la página de aspectos destacados del componente.

=== Filtro

Define los filtros que se muestran en las páginas de resumen.

Los filtros se muestran cuando existen datos coincidentes para los componentes vinculados.

endif::ss-ff-stackpacks2_enabled[]
