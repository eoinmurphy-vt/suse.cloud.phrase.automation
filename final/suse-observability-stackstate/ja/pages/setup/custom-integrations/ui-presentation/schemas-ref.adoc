ifdef::ss-ff-stackpacks2_enabled[]
= コンポーネント・プレゼンテーション・スキーマ
:revdate: 2026-01-08
:page-revdate: {revdate}
:description: コンポーネント・プレゼンテーション・スキーマ

[IMPORTANT]
====
*ステータス*現在開発中。

*空室状況*StackPacks 2.0の機能フラグの裏側。機能を有効にする方法については、xref:/setup/custom-integrations/overview.adoc[カスタム統合の概要を]参照してください。

この文書では、現在の設計意図とハイレベルなコンフィギュレーション・サーフェスについて説明する。

注：動作や能力はまだ実装されていない。

この機能は現在開発中であるため、モデルの進化に伴い、動作、スキーマ、機能が変更される可能性がある。
====

== 概要

このページでは、UIプレゼンテーションモデルで使用される設定スキーマについて説明します。
これは、UIプレゼンテーションの動作を定義する作者のためのリファレンスとして意図されている。

ここで説明するすべてのスキーマは*StackPacks 2.*0の一部であり、現在活発に開発中である。

== UIページ `ComponentPresentation`

以下のスクリーンショットを見れば、`ComponentPresentation` のコンフィギュレーションがどのページと各ページのUIエレメントに関係しているかがわかるだろう。

=== コンポーネント・タイプの概要ページ

以下のスキーマ・リファレンスでは、`overview` のセクションを参照。

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_overview_page.png[概要ページ]

=== コンポーネント・ハイライト・ページ

以下のスキーマ・リファレンスでは、`highlight` のセクションを参照。

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_highlight_page.png[ハイライトページ]

== ComponentPresentation

`ComponentPresentation` は、与えられたモードとコンテキストのUIにおいて、マッチするコンポーネントがどのように表示されるかを定義する。

[,yaml]
----
_type: ComponentPresentation
identifier: urn:...:component-presentation:<presentation-name>
mode: ["observability" | "security" | "all"]

binding:
  _type: ComponentPresentationQueryBinding
  query: string                       # Primitive STQL query used to match components

rank:
  specificity: number                 # Higher values indicate more specific definitions

presentation:
  icon: string                        # Optional icon (base64-encoded)
  name:                               # Optional naming configuration
    plural: string
    singular: string
    title: string

  mainMenu:                           # Optional main menu entry
    group: string                     # Name of an existing MainMenuGroup
    rank: number                      # Optional override within the group

  overview:                           # Optional overview page configuration
    columns:
      - _type: string                 # ViewTypeTableCol*

  highlight:                          # Optional highlight page configuration
    summary:
      ...
    fields:
      - _type: string                 # ComponentTypeField
        fieldId: string
        label:
          title: string
          helpBubbleText: string      # Optional
        valueExtractor:
          _type: string
        display:
          _type: string
    about:
      _type: ComponentTypeAbout
      fields:
        - string

  filter:                             # Optional overview filters
    - _type: FilterField
      name: string
      tagField: string
----

== 識別子

識別子は、{stackstate-product-name} 識別子（すなわち、`urn:...` ）の形式に従う必要がある。詳細はxref:/configure/topology/identifiers.adoc[識別子の]ドキュメントを参照。

== 装丁

バインディングは、`ComponentPresentation` がどのコンポーネントに適用されるかを決定する。

バインディングはコンポーネントのデータに対して評価され、UIの評価時に使用されるため、シンプルで効率的でなければならない。

現在サポートされている`binding` ：

* `ComponentPresentationQueryBinding`STQL クエリ（詳細についてはxref:/develop/reference/k8sTs-stql_reference.adoc[STQL]を参照してください。）

== 順位

`rank` セクションは、一致する複数の`ComponentPresentation` 定義がどのように組み合わされるかを制御する。

Specificityの値が高いほど、より専門的な定義であることを示し、プレゼンテーション動作を構成する際には、低い値よりも優先される。

== プレゼンテーション・セクション

`presentation` 以下のフィールドはすべて任意です。`ComponentPresentation` は、プレゼンテーションの側面のサブセットのみを定義してもよい。

プレゼンテーション・セクションは、複数の定義が同じコンポーネントに適用される場合に構成される。

=== アイコンと名前

UI全体で使用されるアイコノグラフィーとネーミングを定義する。

=== メインメニュー

一致するコンポーネントをメインメニューに表示するかどうかを制御する。

参照するメニューグループは、`MainMenuGroup` を使用して個別に定義する必要があります。

=== 概要

概要表に表示される列を定義する。

カラムは、他のマッチングプレゼンテーションによって定義されたカラムをオーバーライドまたは拡張することができる。

=== ハイライト

ハイライトページに表示されるフィールドとセクションを定義します。

==== 概要

==== フィールド

==== について

コンポーネントのハイライトページのaboutパネルに表示されるフィールドを定義します。

=== フィルター

概要ページに表示されるフィルタを定義します。

バインドされたコンポーネントにマッチするデータが存在する場合、フィルターが表示される。

endif::ss-ff-stackpacks2_enabled[]
