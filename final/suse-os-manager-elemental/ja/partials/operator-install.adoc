== {elemental-product-name} オペレーター
:revdate: 2025-09-08
:page-revdate: {revdate}

`elemental-operator` は管理エンドポイントであり、管理クラスタを実行し、インベントリの作成、マシンの登録などを行う。

Helmパッケージ・マネージャーを使って、elemental-operatorチャートをクラスタにインストールする。

[,console]
----
helm upgrade --create-namespace -n cattle-elemental-system --install elemental-operator-crds oci://registry.suse.com/rancher/elemental-operator-crds-chart

helm upgrade --create-namespace -n cattle-elemental-system --install elemental-operator oci://registry.suse.com/rancher/elemental-operator-chart
----

数秒後、`cattle-elemental-system` ネームスペースにオペレーター・ポッドが表示されるはずだ：

[,console]
----
kubectl get pods -n cattle-elemental-system

NAME                                  READY   STATUS    RESTARTS   AGE
elemental-operator-64f88fc695-b8qhn   1/1     Running   0          16s
----

[IMPORTANT]
.Helm v3.8.0以上が必要です。
----
{elemental-product-name} オペレーター・チャートはOCIレジストリを通じて配布される：Helmはv3.8.0リリースからOCIベースのレジストリを正しくサポートしています。
----


[CAUTION]
.elemental-operator リリース < 1.2.4 からのアップグレード時に、チャートのインストール順序を入れ替える。
----
{elemental-product-name} CRD (version < 1.2.4) を埋め込んだ elemental-operator リリースからアップグレードすると、 elemental-operator-crds チャートのインストールに失敗します。
まずelemental-operatorチャートをアップグレードし、それからelemental-operator-crdsチャートをインストールする必要があります。
----

=== 安定しない設備

上記のHelmチャート以外に、`non-stable`。

* *ステージング：*Githubからの最新のタグ付きリリースを指す。これはxref:quickstarts/quickstart-ui.adoc[次の]ページで説明する。
* *開発：*Githubの「HEADの先端」を指す。これは現在進行中の開発バージョンであり、常に変化している。

[tabs]
=====
ステージングバージョン (x86-64, ARM64 (Raspberry Pi 4))::
+
--
[,console]
----
helm upgrade --create-namespace -n cattle-elemental-system --install elemental-operator-crds oci://registry.opensuse.org/isv/rancher/elemental/staging/charts/rancher/elemental-operator-crds-chart 
helm upgrade --create-namespace -n cattle-elemental-system --install elemental-operator oci://registry.opensuse.org/isv/rancher/elemental/staging/charts/rancher/elemental-operator-chart
----
--

開発版 (x86-64、ARM64 (Raspberry Pi 4))::
+
--
[WARNING]
.リマインダー
====
開発版は本番環境には推奨されません。SlackやGithubのissueを通じたフィードバックを歓迎するが、不安定で実験的な機能が含まれている可能性があり、予告なしに削除されることもある。
====

[,console]
----
helm upgrade --create-namespace -n cattle-elemental-system --install elemental-operator-crds oci://registry.opensuse.org/isv/rancher/elemental/dev/charts/rancher/elemental-operator-crds-chart
helm upgrade --create-namespace -n cattle-elemental-system --install --set image.imagePullPolicy=Always elemental-operator oci://registry.opensuse.org/isv/rancher/elemental/dev/charts/rancher/elemental-operator-chart
----
--
=====

=== 設置オプション

チャートのインストールで設定できるオプションがいくつかあるが、この文書では範囲外である。https://github.com/rancher/elemental-operator/blob/main/.obs/chartfile/elemental-operator-helm/values.yaml[values.yamlで]すべての値を見ることができる。
