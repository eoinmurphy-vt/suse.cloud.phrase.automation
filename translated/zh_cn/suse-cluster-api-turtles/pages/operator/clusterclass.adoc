= 启用群集类
:revdate: 2025-06-30
:page-revdate: {revdate}

在本节中，我们将介绍启用https://cluster-api.sigs.k8s.io/tasks/experimental-features/cluster-class/[*集群类*]功能。

[TIP]
====
在使用 ClusterClass 功能之前，请阅读提供商文档并确认该功能是否受支持。本文档的 "xref:../overview/certified.adoc[认证提供商]"部分包含一个矩阵，显示xref:../overview/certified.adoc#_clusterclass_support_for_certified_providers[群集拓扑支持状态]。
====

== 启用群集类

要使用 ClusterClass 功能，需要启用核心 Cluster API 和任何支持该功能的提供程序。具体做法是将`+CLUSTER_TOPOLOGY+` 变量设置为`+true+` 。

{product_name} 舵手图将在安装 CAPI 时自动启用该功能。不过，在启用其他提供程序时，请确保在提供程序配置中设置`+CLUSTER_TOPOLOGY+` 。Turtles 的xref:../reference/capiprovider.adoc[CAPIProvider]资源支持通过`+variables+` 向提供程序传递安装参数，具体如下：

[source,yaml]
----
apiVersion: turtles-capi.cattle.io/v1alpha1
kind: CAPIProvider
metadata:
  name: azure
  namespace: capz-system
spec:
  type: infrastructure
  name: azure
  configSecret:
    name: azure-variables
  variables:
    CLUSTER_TOPOLOGY: "true"
    EXP_CLUSTER_RESOURCE_SET: "true"
    EXP_MACHINE_POOL: "true"
    EXP_AKS_RESOURCE_HEALTH: "true"
----

该 YAML 文件中定义的资源将安装支持多种功能的 CAPZ，包括`+CLUSTER_TOPOLOGY+` 。
