ifdef::ss-ff-stackpacks2_enabled[]
= 组件显示模式
:revdate: 2026-01-08
:page-revdate: {revdate}
:description: 组件显示模式

[IMPORTANT]
====
*现状*正在积极开发中。

*可用性：*StackPacks 2.0 功能标志的背后。有关如何启用该功能，请参阅xref:/setup/custom-integrations/overview.adoc[自定义集成概述]。

本文档描述了当前的设计意图和高级配置面。

注意：尚未实施任何行为和功能。

由于该功能正在积极开发中，其行为、模式和功能可能会随着模型的发展而改变。
====

== 概述

本页记录了用户界面呈现模型使用的配置模式。
它旨在为定义用户界面表现行为的作者提供参考。

此处描述的所有模式都是*StackPacks 2.0 的*一部分，目前正在积极开发中。

== 用户界面页面由 `+ComponentPresentation+`

请参阅以下截图，了解`+ComponentPresentation+` 配置与哪些页面和相应页面上的用户界面元素有关。

=== 组件类型概览页面

在下面的模式参考中，请参见`+overview+` 部分。

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_overview_page.png[概览页面]

=== 组件亮点页面

在下面的模式参考中，请参见`+highlight+` 部分。

image:custom-integrations/ui-presentation/ui_presentation_schema_ref_highlight_page.png[突出显示页面]

== ComponentPresentation

`+ComponentPresentation+` 定义了在给定模式和上下文中，如何在用户界面中显示匹配组件。

[,yaml]
----
_type: ComponentPresentation
identifier: urn:...:component-presentation:<presentation-name>
mode: ["observability" | "security" | "all"]

binding:
  _type: ComponentPresentationQueryBinding
  query: string                       # Primitive STQL query used to match components

rank:
  specificity: number                 # Higher values indicate more specific definitions

presentation:
  icon: string                        # Optional icon (base64-encoded)
  name:                               # Optional naming configuration
    plural: string
    singular: string
    title: string

  mainMenu:                           # Optional main menu entry
    group: string                     # Name of an existing MainMenuGroup
    rank: number                      # Optional override within the group

  overview:                           # Optional overview page configuration
    columns:
      - _type: string                 # ViewTypeTableCol*

  highlight:                          # Optional highlight page configuration
    summary:
      ...
    fields:
      - _type: string                 # ComponentTypeField
        fieldId: string
        label:
          title: string
          helpBubbleText: string      # Optional
        valueExtractor:
          _type: string
        display:
          _type: string
    about:
      _type: ComponentTypeAbout
      fields:
        - string

  filter:                             # Optional overview filters
    - _type: FilterField
      name: string
      tagField: string
----

== 标识符

标识符应遵循{stackstate-product-name} 标识符（即`+urn:...+` ）格式。更多信息请参阅xref:/configure/topology/identifiers.adoc[标识符]文档。

== 装订

绑定决定了`+ComponentPresentation+` 适用于哪些组件。

绑定是根据组件数据进行评估的，必须简单高效，因为它们是在用户界面评估过程中使用的。

目前支持的`+binding+` 类型：

* `+ComponentPresentationQueryBinding+`： STQL 查询（更多信息请参阅xref:/develop/reference/k8sTs-stql_reference.adoc[STQL）]

== 等级

`+rank+` 部分控制如何组合多个匹配的`+ComponentPresentation+` 定义。

特异性值越高，表示定义越专业，在组成演示行为时优先于较低的特异性值。

== 演示部分

`+presentation+` 下的所有字段均为可选字段。`+ComponentPresentation+` 可以只定义展示方面的一个子集。

当多个定义适用于同一组件时，就会组成演示部分。

=== 图标和名称

定义整个用户界面使用的图标和命名。

=== 主菜单

控制匹配组件是否出现在主菜单中。

必须使用`+MainMenuGroup+` 单独定义引用的菜单组。

=== 概述

定义概览表格中显示的列。

列可以覆盖或扩展其他匹配演示文稿定义的列。

=== 亮点

定义高亮页面上显示的字段和部分。

==== 摘要

==== 字段

==== 关于

定义组件高亮页面 "关于 "面板中显示的字段。

=== 过滤器

定义概览页面显示的筛选器。

如果绑定组件存在匹配数据，则会显示筛选器。

endif::ss-ff-stackpacks2_enabled[]
