= 停止 K3
:page-languages: [EN, JA, KO, ZH]
:revdate: 2024-09-26
:page-revdate: {revdate}

为了在升级期间实现高可用性，当 K3s 服务停止时，K3s 容器会继续运行。

== K3s 服务

systemd 和 OpenRC 的安装脚本支持停止和重启 K3s。

[tabs]
======
systemd::
+
--
停止服务器：

[,sh]
----
sudo systemctl stop k3s
----

重新启动服务器：

[,sh]
----
sudo systemctl start k3s
----

阻止特工

[,sh]
----
sudo systemctl stop k3s-agent
----

重新启动代理：

[,sh]
----
sudo systemctl start k3s-agent
----
--

OpenRC::
+
--
停止服务器：

[,sh]
----
sudo rc-service k3s stop
----

重新启动服务器：

[,sh]
----
sudo rc-service k3s restart
----

阻止特工

[,sh]
----
sudo rc-service k3s-agent stop
----

重新启动代理：

[,sh]
----
sudo rc-service k3s-agent restart
----
--
======

== Killall 脚本

要停止所有 K3s 容器并重置 containerd 状态，可以使用`+k3s-killall.sh+` 脚本。

killall 脚本会清理容器、K3s 目录和网络组件，同时删除带有所有相关规则的 iptables 链。不会删除群集数据。

要从服务器节点运行 killall 脚本，请运行

[,bash]
----
/usr/local/bin/k3s-killall.sh
----
