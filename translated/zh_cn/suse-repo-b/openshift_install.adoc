= 安装 OpenShift
:revdate: 2025-07-10
:page-revdate: {revdate}
:description: {stackstate-product-name} 自托管

== 开始之前

[NOTE]
====
安装额外说明：

* *权限有限的 OpenShift 集群*：读取xref:/setup/install-stackstate/kubernetes_openshift/required_permissions.adoc[所需权限]。
* *Kubernetes*请参阅xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc[Kubernetes 安装说明]。
====


在您开始安装{stackstate-product-name}.NET 之前，请先阅读以下内容

* 检查 OpenShift 环境是否xref:/setup/install-stackstate/requirements.adoc[符合要求]
* 确保已安装 OpenShift 命令行工具 (`+oc+`)
* 将{stackstate-product-name} helm 资源库添加到本地 helm 客户端：

[,text]
----
helm repo add suse-observability https://charts.rancher.com/server-charts/prime/suse-observability
helm repo update
----

== 安装 {stackstate-product-name}

[NOTE]
====
对于没有互联网接入的环境，也称为空气屏蔽环境，首先要遵循xref:/k8s-suse-rancher-prime-air-gapped.adoc[以下额外说明]。

此外，只要有气隙安装说明，就一定要按照说明进行安装。
====


. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_project[创建安装{stackstate-product-name} 的项目]
. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#create-values-yaml[创建`+values.yaml+` 配置文件]
. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_openshift_values_yaml[创建`+openshift-values.yaml+` 文件]
. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_deploy_suse_observability_with_helm[使用 Helm 部署{stackstate-product-name} ]
. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_access_the_suse_observability_ui[访问{stackstate-product-name} 用户界面]
. xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_manually_create_securitycontextconfiguration_objects[手动创建`+SecurityContextConfiguration+` 对象]

=== 创建项目

首先创建要安装{stackstate-product-name} 的项目。在演练中，我们将使用命名空间`+suse-observability+` ：

[,text]
----
oc new-project suse-observability
----

[NOTE]
====
项目名称在`+helm+` 和`+kubectl+` 命令中用作`+--namespace+` 标志中的命名空间名称
====


[#create-values-yaml]
=== 创建`+values.yaml+` 配置文件

`+values.yaml+` 文件包含部署{stackstate-product-name} 所需的所有配置，包括许可证密钥、基本 URL、管理员密码和大小配置文件。

[NOTE]
====
*在您继续之前*请确保您拥有最新版本的舵手图表`+helm repo update+` 。
====

[tabs]
====
建议方法::
+
--
[NOTE]
=====
`+global.suseObservability+` 配置方法从`+2.8.0+` 版本开始提供。对于早期版本，请使用传统方法。
=====

使用`+global.suseObservability+` 配置结构创建`+values.yaml+` 文件：

[source,yaml]
----
global:
  # Optional: Override image registry (defaults to registry.rancher.com)
  # Only needed for air-gapped environments or custom registries
  # imageRegistry: "your-private-registry.example.com"

  # Optional: Override storage class (uses cluster default if not set)
  # storageClass: "your-storage-class"

  suseObservability:
    # Required: Your {stackstate-product-name} license key
    license: "YOUR-LICENSE-KEY"

    # Required: Base URL for {stackstate-product-name}
    # Use http://localhost:8080 if ingress is not configured yet
    baseUrl: "https://observability.example.com"

    # Required: Sizing profile
    # Available: trial, 10-nonha, 20-nonha, 50-nonha, 100-nonha,
    #            150-ha, 250-ha, 500-ha, 4000-ha
    sizing:
      profile: "150-ha"

    # Required: Plain text Admin password
    adminPassword: "your-password"
    # Instead of adminPassword you can provide a bcrypt hashed password with adminPasswordBcrypt
    # Generate with: htpasswd -bnBC 10 "" "your-password" | tr -d ':\n'
    # adminPasswordBcrypt: "$2a$10$..."

    # Optional: Receiver API key (auto-generated if not provided)
    # receiverApiKey: "your-receiver-api-key"

    # Optional: Pull secret for private registry
    # pullSecret:
    #   username: "registry-user"
    #   password: "registry-pass"
----

TIP: 要生成 bcrypt 密码哈希值，请使用 `+htpasswd -bnBC 10 "" "your-password" | tr -d ':\n'+`

有关包括亲缘设置在内的更多配置选项，请参阅xref:/setup/install-stackstate/kubernetes_openshift/affinity.adoc[配置 Kubernetes 亲缘]。

--
旧方法（已废弃）::
+
--
[WARNING]
=====
该方法已被弃用。对于新安装的设备，请使用上述推荐方法。对于使用此方法的现有安装，请参阅xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[迁移指南]以过渡到新的配置格式。
=====

您可以通过运行单独的 Helm 图表生成{stackstate-product-name} `+baseConfig_values.yaml+` 、`+sizing_values.yaml+` 和`+affinity_values.yaml+` 文件。`+suse-observability/suse-observability-values+` 图表。命令行示例如下

[,text]
----
export VALUES_DIR=.
helm template \
  --set license='<your license>' \
  --set baseUrl='<suse-observability-base-url>' \
  --set sizing.profile='<sizing.profile>' \
  suse-observability-values \
  suse-observability/suse-observability-values --output-dir $VALUES_DIR
----

该命令生成的文件`+$VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml+` 、`+$VALUES_DIR/suse-observability-values/templates/sizing_values.yaml+` 和`+$VALUES_DIR/suse-observability-values/templates/affinity_values.yaml+` 包含安装{stackstate-product-name} 舵图所需的配置。

[NOTE]
=====
{stackstate-product-name} 管理员密码将由上述命令自动生成，并在生成的`+basicConfig.yaml+` 文件中以注释形式输出。实际值包含这些密码的`+bcrypt+` 哈希值，以便安全地存储在集群中的 Helm 版本中。
=====


可传递给该图表的值有

[cols="1,1,2"]
|===
| 配置 | 价值 | 说明

| 基本配置生成
| `+basicConfig.generate+`
| 打开或关闭`+basicConfig_values.yaml+` 文件的生成。

| 接收器 API 密钥
| `+receiverApiKey+`
| {stackstate-product-name} 从代理接收数据时使用的 API 密钥。这是一个秘密密钥，应予以保密。如果省略此项，将为您生成一个随机密钥。

| 基本 URL
| `+baseUrl+`
| `+<STACKSTATE_BASE_URL>+`.{stackstate-product-name} 的外部 URL，用户和代理将使用该 URL 进行连接。例如`+https://suse-observability.internal+` 。如果您还没有决定入口配置，请使用`+http://localhost:8080+` 。稍后可在生成的文件中进行更新。

| 用户名和密码**
| `+-u+` `+-p+`
| {stackstate-product-name} 提取图像时使用的用户名和密码。对于 air-gapped 环境，这些必须是本地 docker 注册表的用户名和密码。

| 许可证密钥
| `+license+`
| {stackstate-product-name} 许可证密钥。

| 默认密码
| `+adminPassword+`
| 默认用户（`+admin+` ）访问{stackstate-product-name} 用户界面的密码。或者，您也可以使用`+adminPasswordBcrypt+` 提供 bcrypt 已散列密码。两个选项只能同时使用一个。

| 图像登记处
| `+imageRegistry+`
| {stackstate-product-name} 镜像所在的注册表。如果未提供，默认值为 "quay.io

| 拉动秘密用户名
| `+pullSecret.username+`
| 用于从{stackstate-product-name} 镜像所在的 Docker 注册表中提取镜像的用户名。

| 调出秘密密码
| `+pullSecret.password+`
| 用于从{stackstate-product-name} 镜像所在的 Docker 注册表中提取镜像的密码。

| 确定尺寸
| `+sizing.generate+`
| 打开或关闭`+sizing_values.yaml+` 文件的生成。

| 尺寸规格
| `+sizing.profile+`
| 一次试验、10 公顷、20 公顷、50 公顷、100 公顷、150 公顷、250 公顷、500 公顷。根据这些配置文件，可以生成`+sizing_values.yaml+` 文件，其中包含{stackstate-product-name} 资源和配置的默认大小，以便在 HA 或非 HA 模式下部署。例如，10-nonha 生成`+sizing_values.yaml+` ，用于部署一个非高可用性{stackstate-product-name} 实例，以观察非高可用性模式下的 10 节点集群。目前，从非 HA 环境转移到 HA 环境是不可能的。如果您预计您的环境需要观察 150 个左右的节点，那么最好立即使用 HA。

| Pod AntiAffinity
| `+affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution+`
| 确保相同数据服务的副本调度到不同的 Kubernetes 节点。仅适用于`+HA+` 配置文件。`+true+` 或`+false+` 。默认值为`+true+` - 执行调度到不同节点。
|===

[NOTE]
=====
安全存储生成的`+basicConfig.yaml+` 和`+sizing_values.yaml+` 文件。您可以重复使用这些文件进行升级，从而节省时间，并确保{stackstate-product-name} 继续使用相同的 API 密钥。这样做是可取的，因为这意味着{stackstate-product-name} 的代理和其他数据提供者无需更新。
您可以使用`+basicConfig.generate=false+` 和`+sizing.generate=false+` 开关单独重新生成文件，禁用其中任何一个，同时仍将先前生成的文件版本保留在`+output-dir+` 中。
=====

[NOTE]
=====
{stackstate-product-name} 值图表会生成亲和配置，您可以将其与{stackstate-product-name} 主图表一起使用，以控制 pod 的调度行为。更多信息，请参阅xref:/setup/install-stackstate/kubernetes_openshift/affinity.adoc[配置 Kubernetes Affinities]文档。
=====

--
====

=== 创建 `+openshift-values.yaml+`

由于 OpenShift 的安全模型比普通 Kubernetes 更严格，因此需要禁用部署中的所有标准安全上下文。

创建包含以下内容的 Helm 值文件`+openshift-values.yaml+` ，并将其保存在生成的`+values.yaml+` 文件旁边。其中包含 OpenShift 部署所需的值。

[,yaml]
----
elasticsearch:
  prometheus-elasticsearch-exporter:
    podSecurityContext: ""
  sysctlInitContainer:
    enabled: false
scc:
  enabled: true
----

=== 使用 Helm 部署{stackstate-product-name} 

{stackstate-product-name} 的推荐部署方式是生产就绪、高可用性设置，许多服务冗余运行。如果需要，也可以在非冗余设置中运行{stackstate-product-name} ，即每个服务只有一个副本。仅建议在测试环境中使用此设置。

对于空气密封环境，请遵循空气密封安装说明。

[tabs]
====
建议方法::
+
--

要在 OpenShift 上的高可用性设置中部署{stackstate-product-name} ：

. 部署前
 ** xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_project[创建安装{stackstate-product-name} 的项目]
 ** <<create-values-yaml,创建`+values.yaml+` 配置文件>>
 ** <<_create_openshift_values_yaml,创建 `+openshift_values.yaml+`>>
. 使用以下命令将最新的{stackstate-product-name} 版本部署到`+suse-observability+` 命名空间：

[,text]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values values.yaml \
  --values openshift-values.yaml \
  --set "clickhouse.sidecars[0].securityContext.runAsUser=null" \
suse-observability \
suse-observability/suse-observability
----

--
建议的方法（气隙式）::
+
--

要在 OpenShift 上以 air-gapped 设置部署{stackstate-product-name} ：

. 部署前
 ** xref:/k8s-suse-rancher-prime-air-gapped.adoc[气隙式安装请遵循以下额外说明]。
 ** xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_project[创建安装{stackstate-product-name} 的项目]
 ** <<create-values-yaml,创建`+values.yaml+` 配置文件>>
 ** <<_create_openshift_values_yaml,创建 `+openshift_values.yaml+`>>
. 使用以下命令将最新的{stackstate-product-name} 版本部署到`+suse-observability+` 命名空间：

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values local-docker-registry.yaml \
  --values values.yaml \
  --values openshift-values.yaml \
  --set "clickhouse.sidecars[0].securityContext.runAsUser=null" \
suse-observability \
suse-observability/suse-observability
----

--
旧方法（已废弃）::
+
--

[WARNING]
=====
该方法已被弃用。对于新安装的设备，请使用上述推荐方法。对于使用此方法的现有安装，请参阅xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[迁移指南]以过渡到新的配置格式。
=====

要在 OpenShift 上的高可用性设置中部署{stackstate-product-name} ：

. 部署前
 ** xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_project[创建安装{stackstate-product-name} 的项目]
 ** <<create-values-yaml,生成`+baseConfig_values.yaml+` 和 `+sizing_values.yaml+`>>
 ** <<_create_openshift_values_yaml,创建 `+openshift_values.yaml+`>>
. 使用以下命令将最新的{stackstate-product-name} 版本部署到`+suse-observability+` 命名空间：

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values $VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/sizing_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/affinity_values.yaml \
  --values openshift-values.yaml \
suse-observability \
suse-observability/suse-observability
----

--
传统方法（气隙式）::
+
--

[WARNING]
=====
该方法已被弃用。对于新安装的设备，请使用上述推荐方法。对于使用此方法的现有安装，请参阅xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[迁移指南]以过渡到新的配置格式。
=====

要在 OpenShift 上以 air-gapped 设置部署{stackstate-product-name} ：

. 部署前
 ** xref:/k8s-suse-rancher-prime-air-gapped.adoc[气隙式安装请遵循以下额外说明]。
 ** xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc#_create_project[创建安装{stackstate-product-name} 的项目]
 ** <<create-values-yaml,生成`+baseConfig_values.yaml+` 和 `+sizing_values.yaml+`>>
 ** <<_create_openshift_values_yaml,创建 `+openshift_values.yaml+`>>
. 使用以下命令将最新的{stackstate-product-name} 版本部署到`+suse-observability+` 命名空间：

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values local-docker-registry.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/sizing_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/affinity_values.yaml \
  --values openshift-values.yaml \
  --set "clickhouse.sidecars[0].securityContext.runAsUser=null" \
suse-observability \
suse-observability-A.B.C.tgz
----

--
====

安装完成后，{stackstate-product-name} 版本应在{stackstate-product-name} 命名空间中列出，所有 pod 也应开始运行：

[,text]
----
# Check the release is listed
helm list --namespace suse-observability

# Check pods are running
# It may take some time for all pods to be installed or available
kubectl get pods --namespace suse-observability
----

=== 访问{stackstate-product-name} 用户界面

{stackstate-product-name} 部署完成后，可以检查所有 pod 是否都已启动并运行：

[,bash]
----
kubectl get pods --namespace suse-observability
----

所有 pod 启动后，即可启用端口转发：

[,bash]
----
kubectl port-forward service/suse-observability-router 8080:8080 --namespace suse-observability
----

{stackstate-product-name} 现在，您可在浏览器`+https://localhost:8080+` 上查阅。使用`+admin+` 用户名和`+values.yaml+` 文件中提供的默认密码登录。

接下来的步骤是

* xref:/setup/install-stackstate/kubernetes_openshift/ingress.adoc[将{stackstate-product-name} 暴露在群集之外]
* xref:/k8s-quick-start-guide.adoc[开始监控 Kubernetes 集群]
* 允许xref:/setup/security/authentication/README.adoc[同事访问]。

== 手动创建`+SecurityContextConfiguration+` 对象

如果无法使用管理员账户在 OpenShift 上安装{stackstate-product-name} ，请要求管理员应用以下`+SecurityContextConfiguration+` 对象。

[,yaml]
----
apiVersion: security.openshift.io/v1
kind: SecurityContextConstraints
metadata:
  name: {{ template "common.fullname.short" . }}-{{ .Release.Namespace }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install
    suse-observability.io/note: "Ignored by helm uninstall, has to be deleted manually"
fsGroup:
  type: RunAsAny
groups:
- system:serviceaccounts:{{ .Release.Namespace }}
runAsUser:
  type: RunAsAny
seLinuxContext:
  type: MustRunAs
supplementalGroups:
  type: RunAsAny
volumes:
- configMap
- downwardAPI
- emptyDir
- ephemeral
- persistentVolumeClaim
- projected
- secret
allowHostDirVolumePlugin: false
allowHostIPC: false
allowHostNetwork: false
allowHostPID: false
allowHostPorts: false
allowPrivilegeEscalation: true
allowPrivilegedContainer: false
readOnlyRootFilesystem: false
seccompProfiles:
  - runtime/default
----

== 另见

* 有关其他配置和管理选项，请参阅 Kubernetes 文档 -xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc[管理{stackstate-product-name} Kubernetes 安装]
* xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[现有安装的迁移指南]
