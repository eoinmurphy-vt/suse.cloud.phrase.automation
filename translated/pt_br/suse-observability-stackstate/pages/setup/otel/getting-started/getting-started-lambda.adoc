= Primeiros passos para o AWS Lambda
:revdate: 2025-07-10
:page-revdate: {revdate}
:description: Observabilidade do SUSE

Configuração do monitoramento de uma ou mais funções do AWS Lambda:

* A(s) função(ões) AWS Lambda monitorada(s) (instrumentada(s) usando o Open Telemetry)
* O coletor de telemetria aberto
* SUSE Observability ou SUSE Cloud Observability

image::otel/open-telemetry-collector-lambda.png[Instrumentação do AWS Lambda com o coletor Open Telemetry em execução no Kubernetes]

== O coletor de telemetria aberto

[NOTE]
====
Para uma configuração de produção, é altamente recomendável instalar o coletor, pois ele permite que seu serviço descarregue dados rapidamente e o coletor pode cuidar do tratamento adicional, como novas tentativas, lotes, criptografia ou até mesmo filtragem de dados confidenciais.
====


Instale o coletor OTel (Open Telemetry); neste exemplo, usamos um cluster Kubernetes para executá-lo próximo às funções Lambda. Uma configuração semelhante pode ser feita usando um coletor instalado em uma máquina virtual. A configuração usada aqui atua apenas como um proxy seguro para descarregar dados rapidamente das funções Lambda e é executada em uma infraestrutura de rede confiável.

=== Criar um token de serviço

Há duas maneiras de criar um token de serviço:

* **SUSE Observability UI** - abra o menu principal clicando no canto superior esquerdo da tela e vá para `+StackPacks+` > `+Open Telemetry+`.  Se você não tiver feito isso antes, clique no botão `+INSTALL+`. Clique no botão `+CREATE NEW SERVICE TOKEN+` e copie o valor para sua área de transferência.
* **CLI do SUSE Observability** - consulte xref:/use/security/k8s-service-tokens.adoc#_manage_service_tokens[Gerenciar tokens de serviço]

O valor do token de serviço deve ser usado quando as instruções abaixo mencionarem `+<SERVICE_TOKEN>+`.

=== Criar o namespace e um segredo para o token de serviço

Instale no namespace `+open-telemetry+` e use o token de serviço:

[,bash]
----
kubectl create namespace open-telemetry
kubectl create secret generic open-telemetry-collector \
    --namespace open-telemetry \
    --from-literal=API_KEY='<SERVICE_TOKEN>'
----

=== Configurar e instalar o coletor

Instalamos o coletor com um gráfico Helm fornecido pelo projeto Open Telemetry. Certifique-se de que você tenha configurado o repositório de gráficos do Open Telemetry helm:

[,bash]
----
helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
----

Crie um arquivo de valores `+otel-collector.yaml+` para o gráfico do Helm. Aqui está um bom ponto de partida para uso com o SUSE Observability, substitua `+<otlp-suse-observability-endpoint:port>+` pelo seu endpoint OTLP (consulte a xref:/setup/otel/otlp-apis.adoc[API OTLP] para seu endpoint) e insira o nome do seu cluster Kubernetes em vez de `+<your-cluster-name>+`. Ao usar a configuração de entrada, certifique-se também de inserir seu próprio nome de domínio e o segredo do certificado TLS correspondente nos locais marcados.

.otel-collector.yaml
[,yaml]
----
mode: deployment
presets:
  kubernetesAttributes:
    enabled: true
    # You can also configure the preset to add all the associated pod's labels and annotations to you telemetry.
    # The label/annotation name will become the resource attribute's key.
    extractAllPodLabels: true
extraEnvsFrom:
  - secretRef:
      name: open-telemetry-collector
image:
  repository: "ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-k8s"

config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318
  extensions:
    # Use the API key from the env for authentication
    bearertokenauth:
      scheme: SUSEObservability
      token: "${env:API_KEY}"
  exporters:
    otlp:
      auth:
        authenticator: bearertokenauth
      # Put in your own otlp endpoint, for example otlp-suse-observability.my.company.com:443
      endpoint: <otlp-suse-observability-endpoint:port>

  service:
    extensions: [health_check, bearertokenauth]
    pipelines:
      traces:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]
      metrics:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]
      logs:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp]

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: ingress-traefik-external
    traefik.ingress.kubernetes.io/ingress.class: ingress-traefik-external
    traefik.ingress.kubernetes.io/backend-protocol: GRPC
    # "12.34.56.78/32" IP address of NatGateway in the VPC where the otel data is originating from
    #  traefik.ingress.kubernetes.io/whitelist-source-range: "12.34.56.78/32"
  hosts:
    - host: "otlp-collector-proxy.<your-domain>"
      paths:
        - path: /
          pathType: ImplementationSpecific
          port: 4317
  tls:
    - secretName: <secret-for-tls-certificate>
      hosts:
        - "otlp-collector-proxy.<your-domain>"

# Instead of ingress:

# Alternative 1, load balancer service
#service:
#  type: LoadBalancer
#  loadBalancerSourceRanges: 12.34.56.78/32 # The IP address of NatGateway in the VPC for the lambda functions

# Alternative 2, node port service
#service:
#  type: NodePort
#ports:
#  otlp:
#    nodePort: 30317
----


Agora instale o coletor, usando o arquivo de configuração:

[,bash]
----
helm upgrade --install opentelemetry-collector open-telemetry/opentelemetry-collector \
  --values otel-collector.yaml \
  --namespace open-telemetry
----

Certifique-se de que o coletor de proxy esteja acessível às funções do Lambda, fazendo com que o ingresso seja acessível publicamente ou que o IP do coletor esteja na mesma VPC que as funções do Lambda.  Recomenda-se usar uma lista de permissões de intervalo de origem para filtrar dados de fontes não confiáveis e/ou desconhecidas (consulte o comentário no yaml). Além da configuração de entrada, também é possível expor o coletor às funções Lambda por meio de:

* um serviço LoadBalancer que restringe o acesso limitando os intervalos de origem, consulte "Alternativa 1".
* um serviço NodePort para o coletor, consulte "Alternativa 2".

O coletor oferece muito mais receptores, processadores e exportadores de configuração. Para obter mais detalhes, consulte nossa xref:/setup/otel/collector.adoc[página do coletor]. Para o uso em produção, muitas vezes são geradas grandes quantidades de vãos e você deve começar a configurar a xref:/setup/otel/sampling.adoc[amostragem].

== Instrumentar uma função Lambda

O Open Telemetry suporta a instrumentação de funções Lambda em várias linguagens usando camadas Lambda. A configuração dessas camadas Lambda deve usar o endereço do coletor da etapa anterior para enviar os dados. Para instrumentar um lambda do Node.js, siga nossas xref:/setup/otel/instrumentation/node.js/auto-instrumentation-of-lambdas.adoc[instruções detalhadas aqui]. Para instrumentar outras linguagens, aplique a mesma configuração do Node.js, mas use uma das outras https://opentelemetry.io/docs/platforms/faas/lambda-auto-instrument/[camadas do].

== Veja os resultados

Acesse o SUSE Observability e verifique se o Open Telemetry Stackpack está instalado (no menu principal \-> Stackpacks).

Depois de algum tempo e se as funções do Lambda estiverem recebendo algum tráfego, você poderá encontrar as funções sob o nome do serviço nas visões gerais de Open Telemetry \-> serviços e instâncias de serviço. Os rastreamentos aparecerão no xref:/use/traces/k8sTs-explore-traces.adoc[explorador] de rastreamento e na xref:/use/views/k8s-traces-perspective.adoc[perspectiva de rastreamento] dos componentes de serviço e instância de serviço. As métricas de abrangência e as métricas específicas do idioma (se disponíveis) ficarão disponíveis na xref:/use/views/k8s-metrics-perspective.adoc[perspectiva de métricas] dos componentes.

== Próximas etapas

ifdef::ss-ff-stackpacks2_enabled[]
Você pode adicionar novos gráficos a componentes, por exemplo, o serviço ou a instância de serviço, para seu aplicativo, seguindo xref:/setup/custom-integrations/metric-bindings/index.adoc[nosso guia]. Também é possível criar xref:/use/alerting/k8s-monitors.adoc[novos monitores] usando as métricas e configurar xref:/use/alerting/notifications/configure.adoc[notificações] para ser notificado quando seu aplicativo não estiver disponível ou tiver problemas de desempenho.
endif::[]

ifndef::ss-ff-stackpacks2_enabled[]
Você pode adicionar novos gráficos aos componentes, por exemplo, o serviço ou a instância do serviço, para o seu aplicativo, seguindo o xref:/use/metrics/k8s-add-charts.adoc[nosso guia]. Também é possível criar xref:/use/alerting/k8s-monitors.adoc[novos monitores] usando as métricas e configurar xref:/use/alerting/notifications/configure.adoc[notificações] para ser notificado quando seu aplicativo não estiver disponível ou tiver problemas de desempenho.
endif::[]

== Mais informações

* xref:/use/security/k8s-service-tokens.adoc[Tokens de serviço]
* xref:/setup/otel/otlp-apis.adoc[API de telemetria aberta]
* xref:/setup/otel/collector.adoc[Personalização da configuração do Open Telemetry Collector]
* xref:/setup/otel/instrumentation/README.adoc[SDKs de telemetria abertos]
