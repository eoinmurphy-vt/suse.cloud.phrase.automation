= Primeiros passos com o operador de telemetria aberta no Kubernetes
:revdate: 2025-07-10
:page-revdate: {revdate}
:description: Observabilidade do SUSE

Esta é a configuração que criaremos para um aplicativo que precisa ser monitorado:

* O aplicativo/carga de trabalho monitorado em execução no cluster A, instrumentado automaticamente pelo operador
* O operador Open Telemetry no cluster A
* Um coletor criado pelo operador
* SUSE Observability em execução no cluster B, ou SUSE Cloud Observability

image::otel/open-telemetry-kubernetes-operator.png[Instrumentação de contêineres com autoinstrumentação do operador da Open Telemetry]

== Instalar o operador

O operador Open Telemetry oferece alguns recursos extras em relação à configuração normal do Kubernetes:

* Ele pode instrumentar automaticamente seus pods de aplicativos para as linguagens compatíveis (Java, .NET, Python, Golang, Node.js), sem precisar modificar os aplicativos ou as imagens do docker
* Ele pode ser usado como um substituto para o operador do Prometheus e começar a extrair os endpoints do exportador do Prometheus com base nos monitores de serviços e pods

=== Criar um token de serviço

Há duas maneiras de criar um token de serviço:

* **SUSE Observability UI** - abra o menu principal clicando no canto superior esquerdo da tela e vá para `+StackPacks+` > `+Open Telemetry+`.  Se você não tiver feito isso antes, clique no botão `+INSTALL+`.  Clique no botão `+CREATE NEW SERVICE TOKEN+` e copie o valor para sua área de transferência.
* **CLI do SUSE Observability** - consulte xref:/use/security/k8s-service-tokens.adoc#_manage_service_tokens[Gerenciar tokens de serviço]

O valor do token de serviço deve ser usado quando as instruções abaixo mencionarem `+<SERVICE_TOKEN>+`.

=== Criar o namespace e um segredo para o token de serviço

Instalaremos no namespace `+open-telemetry+` e usaremos o token de serviço:

[,bash]
----
kubectl create namespace open-telemetry
kubectl create secret generic open-telemetry-collector \
    --namespace open-telemetry \
    --from-literal=API_KEY='<SERVICE_TOKEN>'
----

=== Configurar e instalar a operadora

O operador é instalado com um gráfico Helm, portanto, primeiro configure o repositório de gráficos.

[,bash]
----
helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
----

Vamos criar um arquivo `+otel-operator.yaml+` para configurar o operador:

.otel-operator.yaml
[,yaml]
----
# Add image pull secret for private registries
imagePullSecrets: []
manager:
  image:
    # Uses chart.appVersion for the tag
    repository: ghcr.io/open-telemetry/opentelemetry-operator/opentelemetry-operator
  collectorImage:
    # find the latest collector releases at https://github.com/open-telemetry/opentelemetry-collector-releases/releases
    repository: otel/opentelemetry-collector-k8s
    tag: 0.123.0
  targetAllocatorImage:
    repository: ""
    tag: ""
  # Only needed when overriding the image repository, make sure to always specify both the image and tag:
  autoInstrumentationImage:
    java:
      repository: ""
      tag: ""
    nodejs:
      repository: ""
      tag: ""
    python:
      repository: ""
      tag: ""
    dotnet:
      repository: ""
      tag: ""
    # The Go instrumentation support in the operator is disabled by default.
    # To enable it, use the operator.autoinstrumentation.go feature gate.
    go:
      repository: ""
      tag: ""

admissionWebhooks:
  # A production setup should use certManager to generate the certificate, without certmanager the certificate will be generated during the Helm install
  certManager:
    enabled: false
  # The operator has validation and mutation hooks that need a certificate, with this we generate that automatically
  autoGenerateCert:
    enabled: true
----


Agora instale o coletor, usando o arquivo de configuração:

[,bash]
----
helm upgrade --install opentelemetry-operator open-telemetry/opentelemetry-operator \
  --namespace open-telemetry \
  --values otel-operator.yaml
----

Isso apenas instala o operador. Continue a instalar o coletor e a ativar a instrumentação automática.

== O coletor de telemetria aberto

O operador gerencia uma ou mais implantações de coletor por meio de um recurso personalizado do Kubernetes do tipo `+OpenTelemetryCollector+`. Criaremos um usando a mesma configuração usada no xref:/setup/otel/getting-started/getting-started-k8s.adoc[guia de introdução ao Kubernetes].

Ele usa o segredo criado anteriormente no guia. Certifique-se de substituir `+<otlp-suse-observability-endpoint:port>+` por seu endpoint OTLP (consulte a xref:/setup/otel/otlp-apis.adoc[API OTLP] para seu endpoint) e insira o nome de seu cluster do Kubernetes em vez de `+<your-cluster-name>+`:

.collector.yaml
[,yaml]
----
apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: otel-collector
spec:
  mode: deployment
  envFrom:
  - secretRef:
      name: open-telemetry-collector
  # optional service-account for pulling the collector image from a private registries
  # serviceAccount: otel-collector
  config:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
      # Scrape the collectors own metrics
      prometheus:
        config:
          scrape_configs:
          - job_name: opentelemetry-collector
            scrape_interval: 10s
            static_configs:
            - targets:
              - ${env:MY_POD_IP}:8888
    extensions:
      health_check:
        endpoint: ${env:MY_POD_IP}:13133
      # Use the API key from the env for authentication
      bearertokenauth:
        scheme: SUSEObservability
        token: "${env:API_KEY}"
    exporters:
      debug: {}
      nop: {}
      otlp/suse-observability:
        auth:
          authenticator: bearertokenauth
        # Put in your own otlp endpoint, for example otlp-suse-observability.my.company.com:443
        endpoint: <otlp-suse-observability-endpoint:port>
        compression: snappy
    processors:
      memory_limiter:
        check_interval: 5s
        limit_percentage: 80
        spike_limit_percentage: 25
      batch: {}
      resource:
        attributes:
        - key: k8s.cluster.name
          action: upsert
          # Insert your own cluster name
          value: <your-cluster-name>
        - key: service.instance.id
          from_attribute: k8s.pod.uid
          action: insert
          # Use the k8s namespace also as the open telemetry namespace
        - key: service.namespace
          from_attribute: k8s.namespace.name
          action: insert
    connectors:
      # Generate metrics for spans
      spanmetrics:
        metrics_expiration: 5m
        namespace: otel_span
    service:
      extensions: [ health_check,  bearertokenauth ]
      pipelines:
        traces:
          receivers: [otlp]
          processors: [memory_limiter, resource, batch]
          exporters: [debug, spanmetrics, otlp/suse-observability]
        metrics:
          receivers: [otlp, spanmetrics, prometheus]
          processors: [memory_limiter, resource, batch]
          exporters: [debug, otlp/suse-observability]
        logs:
          receivers: [otlp]
          processors: []
          exporters: [nop]
      telemetry:
        metrics:
          address: ${env:MY_POD_IP}:8888
----


[CAUTION]
====
*Use o mesmo nome de cluster usado para instalar o agente do SUSE Observability* se você também usar o agente do SUSE Observability com o stackpack do Kubernetes. O uso de um nome de cluster diferente resultará em uma perspectiva de rastreamento vazia para os componentes do Kubernetes e, em geral, tornará a correlação de informações muito mais difícil para o SUSE Observability e seus usuários.
====


Agora, aplique este `+collector.yaml+` no namespace `+open-telemetry+` para implantar um coletor:

[,bash]
----
kubectl apply --namespace open-telemetry -f collector.yaml
----

O coletor oferece muito mais receptores, processadores e exportadores de configuração. Para obter mais detalhes, consulte nossa xref:/setup/otel/collector.adoc[página do coletor]. Para o uso em produção, muitas vezes são geradas grandes quantidades de vãos e você deve começar a configurar a xref:/setup/otel/sampling.adoc[amostragem].

== Auto-instrumentação

=== Configurar a instrumentação automática

Agora precisamos informar ao operador como configurar a instrumentação automática para os diferentes idiomas usando outro recurso personalizado, do tipo `+Instrumentation+`. É usado principalmente para configurar o coletor que acabou de ser implantado como o ponto de extremidade de telemetria para os aplicativos instrumentados.

Ele pode ser definido em um único local e usado por todos os pods no cluster, mas também é possível ter um `+Instrumentation+` diferente em cada namespace. Faremos o primeiro aqui. Observe que, se você usou um namespace diferente ou um nome diferente para o coletor otel, o endpoint nesse arquivo precisa ser atualizado de acordo.

Crie um `+instrumentation.yaml+`:

.instrumentation.yaml
[,yaml]
----
apiVersion: opentelemetry.io/v1alpha1
kind: Instrumentation
metadata:
  name: otel-instrumentation
spec:
  exporter:
    # default endpoint for the instrumentation
    endpoint: http://otel-collector-collector.open-telemetry.svc.cluster.local:4317
  propagators:
    - tracecontext
    - baggage
  defaults:
    # To use the standard app.kubernetes.io/ labels for the service name, version and namespace:
    useLabelsForResourceAttributes: true
  python:
    env:
      # Python autoinstrumentation uses http/proto by default, so data must be sent to 4318 instead of 4317.
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        value: http://otel-collector-collector.open-telemetry.svc.cluster.local:4318
  dotnet:
    env:
      # Dotnet autoinstrumentation uses http/proto by default, so data must be sent to 4318 instead of 4317.
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        value: http://otel-collector-collector.open-telemetry.svc.cluster.local:4318
  go:
    env:
      # Go autoinstrumentation uses http/proto by default, so data must be sent to 4318 instead of 4317.
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        value: http://otel-collector-collector.open-telemetry.svc.cluster.local:4318
----


Agora, aplique o `+instrumentation.yaml+` também no espaço de nome `+open-telemetry+`:

[,bash]
----
kubectl apply --namespace open-telemetry -f instrumentation.yaml
----

=== Ativar a instrumentação automática para um pod

Para instruir o operador a instrumentar automaticamente seus pods de aplicação, precisamos adicionar uma anotação ao pod:

* Java: `+instrumentation.opentelemetry.io/inject-java: open-telemetry/otel-instrumentation+`
* NodeJS: `+instrumentation.opentelemetry.io/inject-nodejs: open-telemetry/otel-instrumentation+`
* Python: `+instrumentation.opentelemetry.io/inject-python: open-telemetry/otel-instrumentation+`
* Vá: `+instrumentation.opentelemetry.io/inject-go: open-telemetry/otel-instrumentation+`

Observe que o valor da anotação se refere ao namespace e ao nome do recurso `+Instrumentation+` que criamos. Outras opções são:

* "true" - injeta e `+Instrumentation+` recurso personalizado do namespace.
* "my-instrumentation" - nome do recurso personalizado `+Instrumentation+` no namespace atual.
* "my-other-namespace/my-instrumentation" - namespace e nome do recurso personalizado `+Instrumentation+` em outro namespace.
* "false" - não injetar

Quando um pod com uma das anotações é criado, o operador modifica o pod por meio de um gancho de mutação:

* Ele adiciona um contêiner de inicialização que fornece a biblioteca de autoinstrumentação
* Ele modifica o primeiro contêiner do pod para carregar a instrumentação durante a inicialização e adiciona variáveis de ambiente para configurar a instrumentação

Se precisar personalizar quais contêineres devem ser instrumentados, use a https://github.com/open-telemetry/opentelemetry-operator?tab=readme-ov-file#_multi_container_pods_with_multiple_instrumentations[documentação do operador].

[CAUTION]
====
A instrumentação automática do Go requer permissões elevadas. Essas permissões são definidas automaticamente pelo operador:

[,yaml]
----
securityContext:
  privileged: true
  runAsUser: 0
----

====


== Veja os resultados

Acesse o SUSE Observability e verifique se o Open Telemetry Stackpack está instalado (no menu principal \-> Stackpacks).

Depois de um curto período de tempo e se os pods estiverem recebendo algum tráfego, você poderá encontrá-los com o nome do serviço nas visões gerais de Open Telemetry \-> serviços e instâncias de serviço. Os rastreamentos aparecerão no xref:/use/traces/k8sTs-explore-traces.adoc[explorador] de rastreamento e na xref:/use/views/k8s-traces-perspective.adoc[perspectiva de rastreamento] dos componentes de serviço e instância de serviço. As métricas de abrangência e as métricas específicas do idioma (se disponíveis) ficarão disponíveis na xref:/use/views/k8s-metrics-perspective.adoc[perspectiva de métricas] dos componentes.

Se você também tiver o stackpack do Kubernetes instalado, os pods instrumentados também terão os rastreamentos disponíveis na xref:/use/views/k8s-traces-perspective.adoc[perspectiva de rastreamento].

== Rancher RBAC

Para que xref:/setup/security/rbac/rbac_rancher.adoc[o RBAC do Rancher] funcione, os dados de telemetria precisam ter os seguintes atributos de recursos presentes:

* `+k8s.cluster.name+` - o nome *do cluster* conforme usado pelo empilhamento do Kubernetes
* `+k8s.namespace.name+` - um *Namespace* gerenciado por um *projeto* Rancher

O Operador do Kubernetes injetará esses atributos por padrão em todos os dados de telemetria que forem enviados.

== Próximas etapas

ifdef::ss-ff-stackpacks2_enabled[]
Você pode adicionar novos gráficos a componentes, por exemplo, o serviço ou a instância de serviço, para seu aplicativo, seguindo xref:/setup/custom-integrations/metric-bindings/index.adoc[nosso guia]. Também é possível criar xref:/use/alerting/k8s-monitors.adoc[novos monitores] usando as métricas e configurar xref:/use/alerting/notifications/configure.adoc[notificações] para ser notificado quando seu aplicativo não estiver disponível ou tiver problemas de desempenho.
endif::[]

ifndef::ss-ff-stackpacks2_enabled[]
Você pode adicionar novos gráficos aos componentes, por exemplo, o serviço ou a instância do serviço, para o seu aplicativo, seguindo o xref:/use/metrics/k8s-add-charts.adoc[nosso guia]. Também é possível criar xref:/use/alerting/k8s-monitors.adoc[novos monitores] usando as métricas e configurar xref:/use/alerting/notifications/configure.adoc[notificações] para ser notificado quando seu aplicativo não estiver disponível ou tiver problemas de desempenho.
endif::[]

O operador, o `+OpenTelemetryCollector+` e o recurso personalizado `+Instrumentation+` têm mais opções que estão documentadas no https://github.com/open-telemetry/opentelemetry-operator[readme]. Por exemplo, é possível instalar um https://github.com/open-telemetry/opentelemetry-operator?tab=readme-ov-file#_target_allocator[alocador de destino] opcional por meio do recurso `+OpenTelemetryCollector+`, que pode ser usado para configurar o receptor Prometheus do coletor. Isso é especialmente útil quando você deseja substituir o operador do Prometheus e está usando seus recursos personalizados `+ServiceMonitor+` e `+PodMonitor+`.

== Mais informações

* xref:/use/security/k8s-service-tokens.adoc[Tokens de serviço]
* xref:/setup/otel/otlp-apis.adoc[API de telemetria aberta]
* xref:/setup/otel/collector.adoc[Personalização da configuração do Open Telemetry Collector]
* xref:/setup/otel/instrumentation/README.adoc[SDKs de telemetria abertos]
* https://github.com/open-telemetry/opentelemetry-operator[Operador de Telemetria Aberta]
