include::partial$variables.adoc[]
= Verwendung von Pod Security Admission mit {project-name}
:revdate: 2026-03-18
:page-revdate: {revdate}
:description: Konfigurieren Sie Pod Security Admission in einem Kubernetes-Cluster mit {project-name}, um Pods zu sichern.
:doc-persona: ["kubewarden-user", "kubewarden-operator", "kubewarden-distributor", "kubewarden-integrator"]
:doc-topic: ["pod-security-admission-with-kubewarden"]
:doc-type: ["howto"]
:keywords: ["{project-name}", "kubewarden", "pod security admission", "pod security policy", "kubernetes"]
:sidebar_label: Pod Security Zulassung
:sidebar_position: 30
:current-version: {page-origin-branch}

Pod-Sicherheitsrichtlinien (PSP) werden seit der Version 1.25 von Kubernetes entfernt.
Sie werden durch die https://kubernetes.io/docs/concepts/security/pod-security-admission/[Pod Security Admission] (PSA) ersetzt.

PSA vereinfacht die Sicherung der Pods in Kubernetes-Clustern.

PSA hat drei Profile (beschrieben https://kubernetes.io/docs/concepts/security/pod-security-standards/[in Pod Security Standards]) für Namensräume:

* *privilegiert*, mit dem breitesten Spektrum an Berechtigungen
* *Baseline*, um neue Privilegieneskalationen zu verhindern
* *eingeschränkt*, eingeschränkt zu härten Pods

Ein PSA-Controller führt Maßnahmen zur Erkennung von Verstößen durch.
Die Aktionen sind: `+enforce+`, `+audit+`, und `+warn+`.
Sie können konfiguriert werden.

Zum Zeitpunkt der Erstellung dieses Dokuments hat der PSA-Controller mit Kubernetes 1.28 die folgenden Einschränkungen:

* Keine Mutationsfähigkeit
* Objekte höherer Ebenen (wie `+Deployment+`, `+Job+`) werden nur ausgewertet, wenn die Modi `+audit+` oder `+warn+` aktiviert sind.

{short-project-name} kann zur *Integration* eines PSA-Profils verwendet werden, um diese Einschränkungen zu umgehen.

[NOTE]
====

Sie können {short-project-name} verwenden, um die alte PSP-Konfiguration zu ersetzen, wie in xref:howtos/psp-migration.adoc[PSP-Migration] gezeigt.
Ziel dieses Artikels ist es jedoch zu zeigen, wie {short-project-name} die neue PSA ergänzen kann.
====


== Beispiel

In diesem Beispiel erstellen wir einen Namespace und wenden restriktive PSA-Richtlinien an:

[subs="+attributes",shell]
----
kubectl apply -f - <<EOF
apiVersion: v1
kind: Namespace
metadata:
  name: my-namespace
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: v1.25
EOF
----

Mit diesem PSA-Profil ist es nicht möglich, Container zu erstellen, die ihre Anwendung unter dem Benutzer `+root+` ausführen.
Bei der Definition dieses Containers:

* das Attribut `+runAsNonRoot+` muss gesetzt werden auf `+true+`
* die `+runAsUser+` kann nicht auf `+0+` eingestellt werden.

Die folgende Ressource wird also nicht den gewünschten Zustand erreichen:

.`+kubectl+` Befehl zur Konfiguration einer Ressource mit `+runAsUser: 0+` (gekennzeichnet als ➀)
[%collapsible]
=====
[subs="+attributes",shell]
----
kubectl apply -n my-namespace -f - <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: template-nginx
  template:
    metadata:
      labels:
        app: template-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        securityContext:
          runAsNonRoot: true
          runAsUser: 0 <1>
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
          seccompProfile:
            type: "RuntimeDefault"
        ports:
        - containerPort: 80
EOF
----
<1> `+runAsUser: 0+`
=====

Wenn wir die Bereitstellung überprüfen, können wir sehen, dass die PSA die Pod-Erstellung verhindert:

[subs="+attributes",shell]
----
kubectl get deploy -n my-namespace nginx-deployment -o json | jq ".status.conditions[] | select(.reason == \"FailedCreate\")"
{
  "lastTransitionTime": "2022-10-28T19:09:56Z",
  "lastUpdateTime": "2022-10-28T19:09:56Z",
  "message": "pods \"nginx-deployment-5f98b4db8c-2m96l\" is forbidden: violates PodSecurity \"restricted:v1.25\": runAsUser=0 (container \"nginx\" must not set runAsUser=0)",
  "reason": "FailedCreate",
  "status": "True",
  "type": "ReplicaFailure"
}
----

Sie können dies beheben, indem Sie die `+runAsUser: 0+` aus der Containerdefinition entfernen:

.`+kubectl+` Befehl zur Konfiguration einer Ressource ohne `+runAsUser: 0+`
[%collapsible]
=====
[subs="+attributes",shell]
----
kubectl apply -n my-namespace -f - <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: template-nginx
  template:
    metadata:
      labels:
        app: template-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
          seccompProfile:
            type: "RuntimeDefault"

        ports:
        - containerPort: 80
EOF
----
=====

Jetzt erlaubt PSA einen Versuch der Pod-Erstellung, der aber immer noch fehlschlägt.

[subs="+attributes",shell]
----
kubectl get pods -n my-namespace
NAME                                READY   STATUS                       RESTARTS   AGE
nginx-deployment-57d8568bbb-h4bx7   0/1     CreateContainerConfigError   0          47s
----

Das liegt daran, dass in der Containerdefinition kein Benutzer angegeben wurde, der beim Starten eines Programms innerhalb des Containers verwendet wird.
Wenn dies der Fall ist, wird das Programm standardmäßig als Root-Benutzer ausgeführt.
Das ist nach der Richtlinie `+runAsNonRoot+` nicht zulässig:

[subs="+attributes",shell]
----
kubectl get pods -n my-namespace nginx-deployment-57d8568bbb-h4bx7 -o json | jq ".status.containerStatuses"
[
  {
    "image": "nginx:1.14.2",
    "imageID": "",
    "lastState": {},
    "name": "nginx",
    "ready": false,
    "restartCount": 0,
    "started": false,
    "state": {
      "waiting": {
        "message": "container has runAsNonRoot and image will run as root (pod: \"nginx-deployment-57d8568bbb-8mvkc_my-namespace(add7bcc5-3d23-43d0-94e9-6e78f887a53f)\", container: nginx)",
        "reason": "CreateContainerConfigError"
      }
    }
  }
]
----

Hier kann {short-project-name} helfen.
Sie können mit der https://artifacthub.io/packages/kubewarden/user-group-psp/user-group-psp[`+user-group-policy+`] Richtlinie verwenden, um die Einsatzdefinition zu ändern.
Dadurch wird ein Standardbenutzer für Container konfiguriert, der diese Informationen auslässt.

[NOTE]
====

Für dieses Beispiel benötigen Sie den {short-project-name} Stack im Kubernetes-Cluster.
Weitere Einzelheiten finden Sie im xref:quick-start.adoc[QuickStart].
====


Es ist möglich, einen Benutzer-ID-Bereich zu erzwingen, zum Beispiel 1000--2000 und 4000--5000:

.kubectl-Befehl zum Erzwingen eines Benutzer-ID-Bereichs
[%collapsible]
======

[subs="+attributes",shell]
----
kubectl apply -f - <<EOF
apiVersion: policies.kubewarden.io/v1
kind: ClusterAdmissionPolicy
metadata:
  name: user-group-psp
spec:
  policyServer: default
  module: registry://ghcr.io/kubewarden/policies/user-group-psp:latest
  rules:
  - apiGroups: ["", "apps"]
    apiVersions: ["v1"]
    resources: ["pods", "deployments"]
    operations:
    - CREATE
    - UPDATE
  mutating: true
  settings:
    run_as_user:
      rule: "MustRunAs"
      overwrite: false
      ranges:
        - min: 1000
          max: 2000
        - min: 4000
          max: 5000
    run_as_group:
      rule: "RunAsAny"
    supplemental_groups:
      rule: "RunAsAny"
EOF
----

======

Prüfen Sie, ob die Richtlinie aktiv ist, bevor Sie fortfahren:

[subs="+attributes",shell]
----
kubectl get clusteradmissionpolicy.policies.kubewarden.io/user-group-psp
----

Wenn die Richtlinie aktiv ist, erstellen Sie die Bereitstellung neu:

.kubectl-Befehl zur Wiederherstellung des Deployments
[%collapsible]
======

[subs="+attributes",shell]
----
kubectl delete deployment -n my-namespace nginx-deployment && \
kubectl apply -n my-namespace -f - <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: template-nginx
  template:
    metadata:
      labels:
        app: template-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
          seccompProfile:
            type: "RuntimeDefault"

        ports:
        - containerPort: 80
EOF
----

======

Jetzt wird das Deployment durch die Richtlinie von {short-project-name} geändert, die den Start des Pods erlaubt.
Der innerhalb des Pods definierte Container hat einen Standardwert `+runAsUser+`:

[subs="+attributes",shell]
----
kubectl get pods -n my-namespace nginx-deployment-57d8568bbb-nv8fj -o json | jq ".spec.containers[].securityContext"
{
  "allowPrivilegeEscalation": false,
  "capabilities": {
    "drop": [
      "ALL"
    ]
  },
  "runAsNonRoot": true,
  "runAsUser": 1000,
  "seccompProfile": {
    "type": "RuntimeDefault"
  }
}
----

Die Integration von {short-project-name} kann in diesem Szenario mehr leisten.
Sie kann den Wert der bereitgestellten `+runAsUser+` überprüfen.

Diese Ressource wird von der zuvor beschriebenen {short-project-name} Richtlinie abgelehnt:

.kubectl-Befehl zum Anzeigen der Ressourcenablehnung
[%collapsible]
======

[subs="+attributes",shell]
----
kubectl apply -n my-namespace -f - <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment2
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: template-nginx
  template:
    metadata:
      labels:
        app: template-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        securityContext:
          runAsNonRoot: true
          runAsUser: 7000 <1>
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
          seccompProfile:
            type: "RuntimeDefault"
        ports:
        - containerPort: 80
EOF
----
<1> `+runAsUser: 7000+`
======

Sie wird abgelehnt, weil der Wert `+runAsUser+` auf `+7000+` gesetzt ist, was außerhalb der von der Richtlinie erlaubten Bereiche liegt:

[subs="+attributes",shell]
----
kubectl get deploy -n my-namespace nginx-deployment -o json | jq ".status.conditions[] | select(.reason == \"FailedCreate\")"
{
  "lastTransitionTime": "2022-10-28T19:22:04Z",
  "lastUpdateTime": "2022-10-28T19:22:04Z",
  "message": "admission webhook \"clusterwide-user-group-psp.kubewarden.admission\" denied the request: User ID outside defined ranges",
  "reason": "FailedCreate",
  "status": "True",
  "type": "ReplicaFailure"
}
----

== Zusammenfassung

PSA bietet eine einfache Möglichkeit zur Sicherung von Kubernetes-Clustern.
Das Hauptziel von PSA ist Einfachheit und es hat nicht die Leistung und Flexibilität des früheren PSP.

Die Verwendung von {short-project-name} zusammen mit PSA hilft, diese Lücke zu schließen.
