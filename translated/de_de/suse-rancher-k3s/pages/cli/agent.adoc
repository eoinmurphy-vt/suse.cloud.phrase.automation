= k3s-Agent
:page-languages: [en, ja, ko, zh]
:revdate: 2025-01-15
:page-revdate: {revdate}

In diesem Abschnitt erfahren Sie, wie Sie den K3s-Agenten konfigurieren.

Beachten Sie, dass auch auf Servern ein Agent läuft, so dass alle auf dieser Seite aufgeführten Flaggen auch für die Verwendung auf Servern gültig sind.

Optionen sind auf dieser Seite als CLI-Flags dokumentiert, können aber auch als Konfigurationsdateioptionen übergeben werden. Weitere Informationen zur Verwendung von YAML-Konfigurationsdateien finden Sie in der Dokumentation zur xref:installation/configuration.adoc#_configuration_file[Konfigurationsdatei].

== Protokollierung

|===
| Flagge | Standard | Beschreibung

| `+-v+` Wert
| 0
| Nummer für die Ausführlichkeit der Protokollstufe

| `+--vmodule+` Wert
| K.A.
| Durch Kommata getrennte Liste von FILE_PATTERN=LOG_LEVEL-Einstellungen für die dateigefilterte Protokollierung

| `+--log value, -l+` Wert
| K.A.
| Log in Datei

| `+--alsologtostderr+`
| K.A.
| Protokollierung im Standardfehler und in der Datei (falls eingestellt)
|===

== Cluster-Optionen

|===
| Flagge | Umgebungsvariable | Beschreibung

| `+--token value, -t+` Wert
| `+K3S_TOKEN+`
| Für die Authentifizierung zu verwendendes Token

| `+--token-file+` Wert
| `+K3S_TOKEN_FILE+`
| Token-Datei, die für die Authentifizierung verwendet wird

| `+--server value, -s+` Wert
| `+K3S_URL+`
| Zu verbindender Server
|===

== Hörer

|===
| Flagge |  Standard | Beschreibung
| `+--bind-address+`
| 0.0.0.0
| k3s bind adresse
|===

== Daten

|===
| Flagge | Standard | Beschreibung

| `+--data-dir value, -d+` Wert
| "/var/lib/rancher/k3s"
| Ordner zum Halten des Status
|===

== Knotenpunkt

|===
| Flagge | Umgebungsvariable | Beschreibung

| `+--node-name+` Wert
| `+K3S_NODE_NAME+`
| Name des Knotens

| `+--with-node-id+`
| K.A.
| id an den Knotennamen anhängen

| `+--node-label+` Wert
| K.A.
| Registrierung und Start von Kubelet mit einer Reihe von Labels

| `+--node-taint+` Wert
| K.A.
| Registrierung von Kubelet mit einer Reihe von Taints

| `+--protect-kernel-defaults+`
| K.A.
| Kernel-Tuning-Verhalten. Falls gesetzt, Fehler, wenn die Kernel-Tunables von den Kubelet-Standardwerten abweichen.

| `+--selinux+`
| `+K3S_SELINUX+`
| SELinux in containerd aktivieren

| `+--lb-server-port+` Wert
| `+K3S_LB_SERVER_PORT+`
| Lokaler Port für Supervisor-Client-Load-Balancer. Wenn der Supervisor und der Apiserver nicht gemeinsam untergebracht sind, wird ein zusätzlicher Port, der um 1 kleiner ist als dieser Port, für den Apiserver-Client-Load-Balancer verwendet. (Standard: 6444)
|===

== Laufzeit

|===
| Flagge | Standard | Beschreibung

| `+--container-runtime-endpoint+`
| K.A.
| Deaktiviert den eingebetteten containerd und verwendet den CRI-Socket am angegebenen Pfad; bei Verwendung mit --docker wird der Socket-Pfad für docker gesetzt

| `+--default-runtime+`
| K.A.
| Einstellen der Standardlaufzeit in containerd

| `+--image-service-endpoint+`
| K.A.
| Deaktiviert den eingebetteten Containerd-Image-Service und verwendet den Socket des Remote-Image-Service unter dem angegebenen Pfad. Wenn nicht angegeben, gilt der Standardwert --container-runtime-endpoint.

| `+--pause-image+` Wert
| "docker.io/rancher/pause:3.1"
| Angepasstes Pause-Image für containerd oder docker sandbox

| `+--private-registry+` Wert
| "/etc/rancher/k3s/registries.yaml"
| Private Registry-Konfigurationsdatei
|===

== Vernetzung

|===
| Flagge | Umgebungsvariable | Beschreibung

| `+--node-ip value, -i+` Wert
| K.A.
| IP-Adresse, die für den Knoten beworben werden soll

| `+--node-external-ip+` Wert
| K.A.
| Externe IP-Adresse, die für den Knoten bekannt gegeben werden soll

| `+--node-internal-dns+`
| K.A.
| interne DNS-Adressen, die für den Knoten bekannt gegeben werden sollen

| `+--node-external-dns+`
| K.A.
| externe DNS-Adressen, die für den Knoten bekannt gegeben werden sollen

| `+--resolv-conf+` Wert
| `+K3S_RESOLV_CONF+`
| Kubelet resolv.conf file

| `+--flannel-iface+` Wert
| K.A.
| Standardflanschschnittstelle außer Kraft setzen

| `+--flannel-conf+` Wert
| K.A.
| Standardkonfigurationsdatei für Flanell überschreiben

| `+--flannel-cni-conf+` Wert
| K.A.
| Standardkonfigurationsdatei von Flannel cni außer Kraft setzen
|===

== Maßgeschneiderte Fahnen

|===
| Flagge | Beschreibung

| `+--kubelet-arg+` Wert
| Maßgeschneiderte Flagge für Kubelet-Prozess

| `+--kube-proxy-arg+` Wert
| Benutzerdefinierte Flagge für kube-proxy Prozess
|===

== Experimentelle

|===
| Flagge | Beschreibung

| `+--rootless+`
| Wurzellos laufen

| `+--docker+`
| cri-dockerd anstelle von containerd verwenden

| `+--enable-pprof+`
| Aktivieren des pprof-Endpunkts am Supervisor-Port

| `+--prefer-bundled-bin+`
| Gebündelte Userspace-Binärdateien gegenüber Host-Binärdateien bevorzugen

| `+--disable-default-registry-endpoint+`
| Siehe "xref:installation/private-registry.adoc#_default_endpoint_fallback[Standard-Endpunkt-Fallback]".

| `+--vpn-auth+`
| Siehe "xref:networking/distributed-multicloud.adoc#_integration_with_the_tailscale_vpn_provider_experimental[Integration mit dem Tailscale VPN-Anbieter]". 

| `+--vpn-auth-file+`
| Siehe "xref:networking/distributed-multicloud.adoc#_integration_with_the_tailscale_vpn_provider_experimental[Integration mit dem Tailscale VPN-Anbieter]".
|===

== Abgelehnt

|===
| Flagge | Umgebungsvariable | Beschreibung

| `+--no-flannel+`
| K.A.
| Verwenden Sie `+--flannel-backend=none+`

| `+--cluster-secret+` Wert
| `+K3S_CLUSTER_SECRET+`
| Verwenden Sie `+--token+`
|===

[#_node_labels_and_taints_for_agents]
== Knotenbezeichnungen und Taints für Agenten

K3s-Agenten können mit den Optionen `+--node-label+` und `+--node-taint+` konfiguriert werden, wodurch dem Kubelet ein Label und ein Taint hinzugefügt werden. Die beiden Optionen fügen Beschriftungen und/oder Färbungen nur zum Zeitpunkt der Registrierung hinzu, so dass sie nur einmal hinzugefügt und danach nicht mehr durch die Ausführung von K3-Befehlen geändert werden können.

Nachfolgend finden Sie ein Beispiel, das zeigt, wie Sie Beschriftungen und einen Taint hinzufügen:

[,bash]
----
     --node-label foo=bar \
     --node-label hello=world \
     --node-taint key1=value1:NoExecute
----

Wenn Sie Knotenbeschriftungen und -farben nach der Knotenregistrierung ändern möchten, sollten Sie `+kubectl+` verwenden. Einzelheiten zum Hinzufügen von https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/[node labels] taints und https://kubernetes.io/docs/tasks/configure-pod-container/assign-pods-nodes/#add-a-label-to-a-node[.] finden Sie in der offiziellen Kubernetes-Dokumentation

== K3s Agent CLI-Hilfe

____
Wenn eine Option unten in Klammern steht, zum Beispiel `+[$K3S_URL]+`, bedeutet dies, dass die Option als Umgebungsvariable dieses Namens übergeben werden kann.
____

[,bash]
----
NAME:
   k3s agent - Run node agent

USAGE:
   k3s agent [OPTIONS]

OPTIONS:
   --config FILE, -c FILE                     (config) Load configuration from FILE (default: "/etc/rancher/k3s/config.yaml") [$K3S_CONFIG_FILE]
   --debug                                    (logging) Turn on debug logs [$K3S_DEBUG]
   -v value                                   (logging) Number for the log level verbosity (default: 0)
   --vmodule value                            (logging) Comma-separated list of FILE_PATTERN=LOG_LEVEL settings for file-filtered logging
   --log value, -l value                      (logging) Log to file
   --alsologtostderr                          (logging) Log to standard error as well as file (if set)
   --token value, -t value                    (cluster) Token to use for authentication [$K3S_TOKEN]
   --token-file value                         (cluster) Token file to use for authentication [$K3S_TOKEN_FILE]
   --server value, -s value                   (cluster) Server to connect to [$K3S_URL]
   --data-dir value, -d value                 (agent/data) Folder to hold state (default: "/var/lib/rancher/k3s") [$K3S_DATA_DIR]
   --node-name value                          (agent/node) Node name [$K3S_NODE_NAME]
   --with-node-id                             (agent/node) Append id to node name
   --node-label value                         (agent/node) Registering and starting kubelet with set of labels
   --node-taint value                         (agent/node) Registering kubelet with set of taints
   --image-credential-provider-bin-dir value  (agent/node) The path to the directory where credential provider plugin binaries are located (default: "/var/lib/rancher/credentialprovider/bin")
   --image-credential-provider-config value   (agent/node) The path to the credential provider plugin config file (default: "/var/lib/rancher/credentialprovider/config.yaml")
   --selinux                                  (agent/node) Enable SELinux in containerd [$K3S_SELINUX]
   --lb-server-port value                     (agent/node) Local port for supervisor client load-balancer. If the supervisor and apiserver are not colocated an additional port 1 less than this port will also be used for the apiserver client load-balancer. (default: 6444) [$K3S_LB_SERVER_PORT]
   --protect-kernel-defaults                  (agent/node) Kernel tuning behavior. If set, error if kernel tunables are different than kubelet defaults.
   --container-runtime-endpoint value         (agent/runtime) Disable embedded containerd and use the CRI socket at the given path; when used with --docker this sets the docker socket path
   --default-runtime value                    (agent/runtime) Set the default runtime in containerd
   --image-service-endpoint value             (agent/runtime) Disable embedded containerd image service and use remote image service socket at the given path. If not specified, defaults to --container-runtime-endpoint.
   --pause-image value                        (agent/runtime) Customized pause image for containerd or docker sandbox (default: "rancher/mirrored-pause:3.6")
   --snapshotter value                        (agent/runtime) Override default containerd snapshotter (default: "overlayfs")
   --private-registry value                   (agent/runtime) Private registry configuration file (default: "/etc/rancher/k3s/registries.yaml")
   --disable-default-registry-endpoint        (agent/containerd) Disables containerd fallback default registry endpoint when a mirror is configured for that registry
   --nonroot-devices                          (agent/containerd) Allows non-root pods to access devices by setting device_ownership_from_security_context=true in the containerd CRI config
   --node-ip value, -i value                  (agent/networking) IPv4/IPv6 addresses to advertise for node
   --bind-address value                       (listener) k3s bind address (default: 0.0.0.0)
   --node-external-ip value                   (agent/networking) IPv4/IPv6 external IP addresses to advertise for node
   --node-internal-dns value                  (agent/networking) internal DNS addresses to advertise for node
   --node-external-dns value                  (agent/networking) external DNS addresses to advertise for node
   --resolv-conf value                        (agent/networking) Kubelet resolv.conf file [$K3S_RESOLV_CONF]
   --flannel-iface value                      (agent/networking) Override default flannel interface
   --flannel-conf value                       (agent/networking) Override default flannel config file
   --flannel-cni-conf value                   (agent/networking) Override default flannel cni config file
   --kubelet-arg value                        (agent/flags) Customized flag for kubelet process
   --kube-proxy-arg value                     (agent/flags) Customized flag for kube-proxy process
   --enable-pprof                             (experimental) Enable pprof endpoint on supervisor port
   --rootless                                 (experimental) Run rootless
   --prefer-bundled-bin                       (experimental) Prefer bundled userspace binaries over host binaries
   --docker                                   (agent/runtime) (experimental) Use cri-dockerd instead of containerd
   --vpn-auth value                           (agent/networking) (experimental) Credentials for the VPN provider. It must include the provider name and join key in the format name=<vpn-provider>,joinKey=<key>[,controlServerURL=<url>][,extraArgs=<args>] [$K3S_VPN_AUTH]
   --vpn-auth-file value                      (agent/networking) (experimental) File containing credentials for the VPN provider. It must include the provider name and join key in the format name=<vpn-provider>,joinKey=<key>[,controlServerURL=<url>][,extraArgs=<args>] [$K3S_VPN_AUTH_FILE]
   --disable-apiserver-lb                     (agent/networking) (experimental) Disable the agent client-side load-balancer and connect directly to the configured server address
----
