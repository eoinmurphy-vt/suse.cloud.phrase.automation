= Transaktionale Elemente
:revdate: 2025-10-27
:page-revdate: {revdate}
:description: SUSE Observabilität

== Übersicht

Auf dieser Seite werden die genauen JSON-Nachrichten beschrieben, die für das Konsistenzmodell der Health-Synchronisierung "Transactional Increments" gesendet werden können.

== JSON-Eigenschaft: "Gesundheit"

Der Gesundheitszustand kann über die Eigenschaft `+"health"+` des xref:/configure/health/send-health-data/send-health-data.adoc#_common_json_object[gemeinsamen JSON-Objekts] an die SUSE Observability Receiver API gesendet werden.

[tabs]
====
Beispiel Gesundheit `+transactional_increments+` JSON::
+
--

[,javascript]
----
   "apiKey":"your api key",
   "collection_timestamp":1585818978,
   "internalHostname":"lnx-343242.srv.stackstate.com",
   "events":{},
   "metrics":[],
   "service_checks":[],
   "health":[
      {
        "consistency_model": "TRANSACTIONAL_INCREMENTS",
        "increment": {
              "checkpoint": {
                  "offset": 5,
                  "batch_index": 102
              },
              "previous_checkpoint": {
                  "offset": 5,
                  "batch_index": 100
              }
        },
        "stream": {
          "urn": "urn:health:sourceId:streamId"
          //"sub_stream_id": "subStreamId" Optional
        },
        "check_states": [
          {
            "checkStateId": "checkStateId1",
            "message": "Server Running out of disk space",
            "health": "Deviating",
            "topologyElementIdentifier": "server-1",
            "name": "Disk Usage"
          },
          {
            "checkStateId": "checkStateId2",
            "message": "Provisioning failed. [Learn more](https://www.any-link.com)",
            "health": "critical",
            "topologyElementIdentifier": "server-2",
            "name": "Health monitor"
          },
          {
            "checkStateId": "checkStateId3",
            "delete": true
          }
        ]
      }
   ],
   "topologies":[]
----

--
====

Jeder Health Transactional Increments Daten-Payload hat die folgenden Details:

* *increment* - Ein Inkrement-Objekt muss in jeder Nachricht vorhanden sein. Auf diese Weise kann SUSE Observability die gesamte Nachrichtenkette verfolgen und erkennen, wann eine erneute Übertragung von Daten oder eine unerwartete Datenlücke auftritt. Sie enthält die folgenden Felder als inkrementelle Metadaten:
 ** *checkpoint* - Objekt, das den Kontrollpunkt bereitstellt, der zu der in der Nachricht enthaltenen `+check_states+` gehört; es hat zwei Felder:
  *** *offset* - Der Offset, der den Nachrichten von der Streaming-Pipeline zugewiesen wird. Zum Beispiel, Kafka Offset.
  *** *batch_index* - Optional. Bei der Verwendung einer einzigen Nachricht, um mehrere `+check_states+` zu akkumulieren, stellt der Batch-Index den neuesten Index dar, der in der Nachricht vorhanden ist, so dass große Batches in separaten Api-Aufrufen gesendet werden können.
 ** *previous_checkpoint* - Optional. Stellt den zuvor übermittelten Kontrollpunkt dar, kann bei der allerersten Übertragung auf dem Teilstrom leer sein. So kann SUSE Observability verfolgen, ob Daten aus dem Upstream fehlen.
* *stream* - Objekt, das Aufschluss darüber gibt, welche Snapshots und `+check_states+` zusammengehören. Sie umfasst die folgenden Felder:
 ** *urn* - Datenquellen- und Stream-ID, kodiert als SUSE Observability xref:/configure/topology/identifiers.adoc[URN], die der folgenden Konvention entspricht: `+urn:health:<sourceId>:<streamId>+`, wobei `+<sourceId>+` der Name der externen Datenquelle und `+<streamId>+` ein eindeutiger Bezeichner für den Gesundheitsdatenstrom ist.
 ** *sub_stream_id* - Optional. Kennung für eine Teilmenge der Stream-Health-Daten. Wenn die Datenströme verteilt und von mehreren Agenten gemeldet werden, ermöglicht dies Schnappschuss-Lebenszyklen pro `+sub_stream_id+`
* *check_states* - Eine Liste von Prüfzuständen. Jeder Prüfstatus kann die folgenden Felder enthalten:
 ** *checkStateId* - Kennung für den Prüfstatus im externen System
 ** *Nachricht* - Optional. Meldung, die in der SUSE Observability-Benutzeroberfläche angezeigt werden soll. Die Daten werden als Markdown interpretiert, so dass Links zu der externen Systemprüfung, die den Status der externen Prüfung erzeugt hat, möglich sind.
 ** *health* - Einer der folgenden SUSE Observability Health-Statuswerte: `+Clear+`, `+Deviating+`, `+Critical+`.
 ** *topologyElementIdentifier* - Wird verwendet, um den Prüfstatus an ein SUSE Observability-Topologieelement zu binden.
 ** *name* - Name des externen Prüfstatus.
 ** *delete* - Flagge, die als Löschanfrage für die zugehörige `+checkStateId+` interpretiert wird. Auch wenn die übrigen Felder für das Anlegen vorhanden sind, hat z. B. `+name, health, ...+` den Vorrang vor dem Löschen.

== Senden von Daten an SUSE Observability

Der Gesundheitszustand kann in einer JSON-Nachricht per HTTP POST übermittelt werden. Im folgenden Beispiel wird ein Snapshot mit zwei Prüfstatus von einem einzigen externen Überwachungssystem an SUSE Observability gesendet.

[,bash]
----
curl -X POST \
 '<STACKSTATE_RECEIVER_API_ADDRESS>' \
 -H 'Content-Type: application/json' \
 -d '{
  "collection_timestamp": 1548857167,
  "internalHostname": "local.test",
  "health": [
    {
      "consistency_model": "TRANSACTIONAL_INCREMENTS",
      "increment": {
            "checkpoint": {
                "offset": 5,
                "batch_index": 102
            },
            "previous_checkpoint": {
                "offset": 5,
                "batch_index": 100
            }
      },
      "stream": {
        "urn": "urn:health:sourceId:streamId"
      },
      "check_states": [
        {
          "checkStateId": "checkStateId1",
          "message": "Server Running out of disk space",
          "health": "Deviating",
          "topologyElementIdentifier": "server-1",
          "name": "Disk Usage"
        },
        {
          "checkStateId": "checkStateId2",
          "message": "Provisioning failed. [Learn more](https://www.any-link.com)",
          "health": "critical",
          "topologyElementIdentifier": "server-2",
          "name": "Health monitor"
        },
        {
          "checkStateId": "checkStateId3",
          "delete": true
        }
      ]
    }
  ]
}'
----
